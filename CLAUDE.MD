# CLAUDE.md — Browser Engine Project

Project directive for Claude Code. Read this in full before making changes. This file governs scope, architecture, and — critically — which parts of this codebase are safe to modify freely versus which require explicit human sign-off regardless of how confident the change looks.

## Current phase: shared engine core, with both front-ends live

Two front-ends now sit on one shared engine core:

1. **Headful, human-facing browser** (`shell`) — a GUI app with tabs, navigation, and rendering (headless `render` to PNG + interactive `winit`/`wgpu` `browse` window).
2. **Headless agentic browser** (`agent`) — a page driver an LLM can operate (navigate / click / scroll / screenshot / observe), driven by a provider-agnostic inference backend. This phase was explicitly authorized and is now in an initial, working form.

The common engine core (networking, parsing, CSS, DOM/JS, layout, text) is realized in the `engine/*` crates and the shared `engine/page` pipeline; the two front-ends diverge only at how they consume a rendered page (window present vs. screenshot+read). Keep that split clean — do not wire front-end-only assumptions into the parse/layout/JS layers.

`.env` holds `GROQ_API_KEY` variables for the agent phase; `.env` is gitignored. The committed agent runner uses a **single** `GROQ_API_KEY` (falling back to `GROQ_API_KEY`); the local-only development harness is a **local-only, gitignored** test tool. The agent's inference backend is decoupled behind an `InferenceBackend` trait — Groq is one implementation, not a hard dependency of the agent logic.

## Current state (implemented)

A working foundation, not a finished browser. What exists and is verified:

- **Networking** (`engine/net`): pooled `hyper-util` client over a `hyper-rustls` connector (no OpenSSL), **HTTP/2 auto-negotiated** (ALPN), **connection reuse**, **gzip/br/deflate decoding** (`async-compression`), redirects, and a general `request` (POST) reused by the agent's Groq client. Live-fetches real sites. *(P0.4 landed.)*
- **Parse → render pipeline** (`engine/{html,dom,css,layout,text,paint}` via `engine/page`): `html5ever` → arena DOM → minimal CSS cascade (selectors/specificity/inheritance) → from-scratch block + inline layout (wrapping, centering, **adjacent-sibling margin collapsing**, **floats** via a BFC-aware `FloatContext` with clearance/shrink-to-fit/text-flow-around, **relative/absolute/fixed positioning** against the containing-block chain, **tables** with fixed/auto column sizing; `taffy` for flex) → `fontdb`/`fontdue` text → `tiny-skia` CPU raster to PNG. Renders real pages (example.com and local pages) end to end.
- **JS** (`engine/js`): `JsRuntime` trait with a no-op default; **SpiderMonkey** (`mozjs`) behind `--features spidermonkey` **actually evaluates JS**. Process-global engine, shared across isolates. Modification boundary respected (see below).
- **CSS engine seam** (`engine/css`): minimal cascade default; **Stylo** behind `--features stylo` (links and builds; adapter currently delegates).
- **Compositor** (`engine/compositor`): tab render/memory tiers (focused-GPU / background-CPU / hibernated), damage tracking, scroll.
- **Headful shell** (`shell`): `render <url>` (headless PNG) and `browse <url>` (winit/wgpu window presenting the CPU raster).
- **Headless agent** (`agent`): `AgentBrowser` driver, `InferenceBackend` trait (multimodal), `run_task` loop, `GroqBackend`. Live-tested on Groq `qwen/qwen3.6-27b`: text extraction, link comprehension, link navigation, and multimodal screenshot reading all pass.
- **Conformance** (`tests/wpt`): built-in layout reftests run today; the upstream WPT runner is the growth path. 40 workspace tests pass; zero warnings.

## Mission

Build a browser engine, from first principles in Rust, that:
- Targets the **broadest possible set of real-world websites**, with the explicit exception of the smallest tier of adversarially hostile sites (aggressive bot-wall/fingerprint-gated sites like LinkedIn, X, Indeed — these are out of scope for the core engine and would be handled by narrow, separate adapters in a different phase, never by chasing generic compatibility with their defenses).
- Is verified against **Web Platform Tests (WPT)**, not against matching Chromium's specific undocumented quirks. Standards compliance is the compatibility target. Do not write code whose purpose is to replicate a Chromium-specific non-standard behavior — if a target site depends on that, it's in the excluded hostile/non-standard tier.
- Is optimized from first principles for binary size, per-tab memory, GPU-quality rendering, and click-to-navigate latency — not merely "acceptable," but genuinely disruptive relative to Chromium/Firefox on these dimensions for the sites in scope.
- Runs identically on Linux, macOS, and Windows with no platform serving as the "primary" target and others as afterthoughts.

## Architecture — the stack

Reuse mature components where reinventing them adds security/correctness risk with no differentiation value. Build from scratch, verified against WPT, where the target site set is bounded and a from-scratch build is both tractable and gives real performance upside. Do not blur this line without discussion — see "Modification boundaries" below.

| Layer | Approach | Components |
|---|---|---|
| Networking | Reuse | `hyper` (HTTP/1.1, HTTP/2), `quinn` (HTTP/3/QUIC), `tokio-tungstenite` (WebSocket), `rustls` (TLS — pure Rust, no OpenSSL system dependency), `tokio` async runtime |
| HTML parsing | Reuse | `html5ever` |
| CSS parsing + cascade | Reuse | Stylo (Servo's/Firefox's production CSS engine) |
| Layout | Reuse + build | `taffy` for flexbox/grid; custom block, inline, table, positioned, and float layout built from scratch, verified against WPT layout reftests |
| DOM + Web API surface | Build | Custom, spec-driven (WebIDL), verified per-API against its WPT test suite. This is the largest-volume component — budget accordingly |
| JS engine | Reuse, do not reimplement | SpiderMonkey, embedded via `mozjs` / `mozjs-sys` Rust bindings (the same integration path Servo uses). **Not V8.** SpiderMonkey is C++ and Rust (Rust in the parser layer only — the JIT and GC are C++); this is not a "pure Rust" engine, it is a mature C++ engine with a proven Rust FFI layer |
| Text shaping | Reuse | Parley + swash + fontdb |
| Paint / rasterization | Reuse | Vello — GPU-compute renderer via `wgpu`, with Vello CPU / Vello Hybrid as lighter fallback tiers for background/hibernated tabs. **Currently alpha upstream** — prototype against actual target rendering needs (gradients, blur, complex clips) before committing further |
| GPU abstraction | Reuse | `wgpu` — Vulkan (Linux/Windows), Metal (macOS), DX12 (Windows). No Linux-desktop or specific graphics-package assumptions |
| Windowing / event loop | Reuse | `winit` |
| Compositor | Build | Layer compositing, scroll, damage tracking, animations — built on Vello + wgpu |
| Concurrency/process model | Build | Isolate/compartment-per-tab (SpiderMonkey compartments) multiplexed across a small number of OS processes — not one OS process per tab. Share startup snapshots across isolates to cut per-tab baseline memory |

## Compatibility verification methodology

- Ground truth is **WPT (Web Platform Tests)** — the cross-browser conformance suite used by Chromium, Firefox, WebKit, and Servo themselves. Every layout, CSS, and DOM/API feature you implement should be checked against the relevant WPT subset before being considered done.
- Do not hand-roll a compatibility test corpus from scratch as the primary verification method — WPT already exists and is comprehensive. Use targeted real-site screenshot diffing (against a real Chrome render) as a secondary check for visual fidelity, not as the primary conformance signal.
- When a feature is ambiguous or under-specified even in WPT (this happens — floats and table layout are notorious), document the interpretation chosen and why, rather than silently picking one.

## Modification boundaries — read before touching the JS engine

This is the most important section in this file. Violating it is the one mistake in this project with real security consequences.

**Safe to modify, verify by benchmark or binary size:**
- SpiderMonkey build configuration and feature flags (stripping unused WASM support, the debugger/inspector protocol, unused ICU locale data — all legitimate binary-size wins)
- The FFI/binding layer between Rust and SpiderMonkey (`mozjs` usage, custom bindings) — this is well-specified, low-blast-radius, and the correct place for iteration
- Startup snapshot tuning (shared heap contents for fast isolate startup)

**Do not modify without dedicated fuzzing infrastructure and explicit sign-off:**
- SpiderMonkey's JIT compiler internals (WarpMonkey/IonMonkey) or garbage collector internals
- Any change to how untrusted JavaScript is sandboxed or isolated

Reasoning, so this isn't just an arbitrary rule: JIT miscompilation bugs — incorrect type or escape-analysis assumptions in the optimizing compiler — are historically the largest single source of exploitable browser RCE vulnerabilities. The reason SpiderMonkey and V8 are trustworthy today isn't just that the code is correct, it's that the code has survived years of adversarial fuzzing (OSS-Fuzz/ClusterFuzz) that this project has no equivalent of. Faster code generation does not substitute for that calendar-bound adversarial exposure. If a task seems to require touching this layer, stop and flag it rather than proceeding — this is a "come back to the human" boundary, not a "do it carefully" one.

## Performance targets

- **Binary size**: static linking + LTO + stripped symbols; `panic = "abort"` in release profile; musl target on Linux for a fully static binary with no dynamic-link bloat; static CRT linking on Windows to avoid requiring a redistributable runtime.
- **Per-tab memory**: isolate-based tabs, not process-based. Background/hibernated tabs should freeze JS execution and evict to disk, dropping to Vello CPU or no active renderer at all — full Vello GPU + active JS is reserved for the focused tab only.
- **Click-to-navigate latency**: streaming/incremental parse and layout (begin building the DOM and layout tree as bytes arrive, don't wait for the full response); zero-copy handoff from socket buffer to tokenizer where feasible; speculative preconnect/DNS-prefetch on link hover.
- **Rendering quality**: Vello + wgpu is the graphics-quality lever — this is a from-scratch, GPU-compute-first architecture, not a CPU/GPU hybrid retrofit. Treat visual fidelity and frame timing as first-class, measured metrics, not an afterthought to functional correctness.

## Portability

- Default async I/O path (`tokio`'s standard reactor) must work identically on Linux, macOS, and Windows. Do not introduce Linux-only dependencies into the core engine.
- `io_uring`-based optimization (`tokio-uring` or similar) is Linux-only and is a *future, optional* fast path relevant to the eventual agent/headless phase, not this one. Do not add it to the human-operator GUI codebase now — the concurrency profile doesn't need it here.
- No Linux-desktop assumptions (no GTK/Qt bindings, no specific display-server assumptions) — `winit` and `wgpu` already abstract this; keep it that way.

## Non-goals

- No adapters for LinkedIn, X, Indeed, or other bot-walled sites; no bot-evasion in the core engine.
- No pixel-parity chase with Chromium beyond what WPT compliance already gets you.
- No DRM/EME.
- No horizontal multi-node scaling concerns — this is single-binary, single-machine scope.
- The agent must not couple to a specific inference provider — Groq is one `InferenceBackend`, not a dependency of the agent logic. Do not commit the local-only development harness or any API keys.

## Next frontiers

Roughly in priority order, each behind an existing seam:

- **Layout**: floats, tables, absolute/relative positioning, margin collapsing — the from-scratch WPT-verified work called out above.
- **Loading**: external `<link rel=stylesheet>` + image fetching, gzip/br decoding, encoding sniffing, HTTP/2+3 activation (ALPN `h2`, `quinn`).
- **Real reuse integrations**: wire Stylo's cascade behind the existing `StyleEngine` adapter; wire Vello GPU paint behind the existing `Painter` trait for the focused tab.
- **JS/DOM**: grow the Web API binding surface (`engine/js::bindings`) projecting the DOM into SpiderMonkey, WebIDL-driven, WPT-verified per API.
- **Agent**: viewport-clipped text observation (so scrolling matters to the text channel, not just the screenshot), richer actions (typing, form submit, back/forward), and multi-tab agent sessions.
- **Conformance**: the upstream WPT runner (`$WPT_DIR`), folding real `css`/`dom` results into the existing `Report`.

---

The above is the near-term list; the comprehensive, objective-organized roadmap
follows. Reviewed and re-prioritized periodically. The north star is a
**disruptively good single-machine browser**: fast, versatile across the real web,
and lightweight — measured, not merely "acceptable." Each item sits behind an
existing seam so it is additive.

### 1. Performance (single machine)
- **Click-to-navigate latency:** wire the incremental path already positioned —
  stream socket bytes into the html5ever sink, build DOM/layout as bytes arrive
  instead of after full buffering; speculative preconnect/DNS-prefetch on link
  hover; connection reuse/pooling (currently one connection per request).
- **Frame timing + rendering quality:** land the **Vello** GPU-compute tier behind
  the existing `Painter` trait for the focused tab; treat frame time and visual
  fidelity as first-class measured metrics.
- **Per-tab memory:** realize hibernation for real — freeze background JS, evict
  rasterized tiles to disk, drop background tabs to Vello-CPU/no-renderer; share
  SpiderMonkey startup snapshots across isolates.
- **Incrementality:** damage-driven relayout + repaint (the compositor already
  tracks damage) instead of full-page passes; dirty-subtree style invalidation.
- **Caching:** HTTP cache, decoded-image cache, shaped-run cache.

**Research dispositions (2026-07, primary sources cited inline):**
- **B1 streaming parse→layout.** *Finding:* html5ever's `parse_document` returns a `Parser: TendrilSink` already exposing `process(chunk)`/`finish()` (push-incremental — docs.rs/html5ever); browsers additionally run a *secondary preload scanner* over raw bytes for `<img>/<link>/<script>` while the main parser blocks on sync scripts/CSS, and CSS is render-blocking so first paint is gated on `<head>` CSS (web.dev/preload-scanner; MDN speculative parsing). *Disposition:* **reuse** html5ever's incremental sink; **hand-roll** the first-paint checkpoint (small engine glue); **defer** the preload scanner until external-resource fetch (D4) exists. Prereq: a chunked-body stream in `engine/net` (today `BodyExt::collect` buffers the whole body). *Roadmap:* the §1 "Click-to-navigate latency" bullet's "stream socket bytes into the html5ever sink" resolves to: net yields body chunks → `parser.process()` per chunk → first-paint checkpoint after `<head>` CSS → incremental body layout. *Pass-2 resolution (2026-07) — the three open pieces closed:* **(1) Net chunked-body stream API:** consume the response `Incoming` body via **`http_body_util::BodyExt::frame().await`** per chunk (each `Frame` is a `Bytes` data chunk) — *not* `collect()` — and feed each chunk to `parser.process(chunk)`; `async-compression` (B3) wraps the chunk stream *before* the parser for `Content-Encoding` (docs.rs/http-body-util BodyExt). **(2) First-paint checkpoint:** DOM construction is incremental, but the initial render is **render-blocking on CSS** (inline `<head>` `<style>` + `<link>`) *by design, to prevent FOUC* — so the checkpoint = **once `<head>` and its render-blocking CSS are processed (CSSOM ready), do the first layout+paint of the DOM-so-far (at `<body>` start); thereafter re-layout/re-paint incrementally per streamed chunk via A2** (web.dev critical-path/FCP; MDN critical-rendering-path). **(3) Net redesign (folds in B2's connector):** replace the manual `conn::http1::handshake` with **`hyper_util::client::legacy::Client` + `hyper-rustls::HttpsConnector`** (`HttpsConnectorBuilder`, ALPN `h2,http/1.1`) — one stack that unifies **pooling/happy-eyeballs (B2) + h2 (B3) + a streamable `Incoming` body via `.frame()` (B1)**; the Client is cheap-to-clone with a reused pool (docs.rs/hyper-rustls, hyper-util `legacy::Client`). *Disposition:* **reuse the whole stack** (`hyper-util` Client + `hyper-rustls`), stream via `.frame()`, **hand-roll** the render-blocking-CSS-gated first-paint checkpoint; incremental relayout is A2; preload scanner stays deferred on D4. No WPT target (perf/architecture). **First-paint checkpoint landed (2026-07-10):** `manuk_html::StreamParser` drives html5ever incrementally (`parse_document` → `Parser::process(chunk)`, snapshotting the parsed-so-far tree by cloning the `RcDom` document `Rc` handle before the parser takes it), with `body_started()` marking the head-complete checkpoint. `Page::load_streaming(chunks, …)` lays out the DOM-so-far at that checkpoint → the **first paint**, returned in `StreamingLoad{first_paint, page}` before the tail is parsed. **Measured (bench/BASELINE.md):** on a head+fold prefix vs a 400-paragraph document, first paint is **13.6 µs vs 1.55 ms full-load — ~113× sooner**. **Remaining for end-to-end over a real slow socket:** a chunked-body fetch in `manuk-net` (`fetch()` buffers via `BodyExt::collect` today; the `.frame()`-per-chunk stream above is the drop-in) + wiring `load_streaming` into the shell; incremental per-chunk relayout reuses A2; the preload scanner + speculative preconnect (hover-warmed socket, same-origin, privacy-constrained) stay deferred. **Chunked-fetch enabler landed (2026-07-10) — streaming is now END-TO-END over the network:** `manuk_net::fetch_streaming(url, on_chunk)` follows redirects then delivers the `Content-Encoding`-decoded body to the sink **as chunks arrive** (via a shared `body_reader`+`wrap_decoder` over `BodyStream`, read in 16 KB chunks — no `collect()` buffering), returning `ResponseMeta`. `StreamParser` is now **byte-based** (html5ever `Utf8LossyDecoder`, so multi-byte chars split across chunk boundaries are handled). `manuk_page::fetch_streaming_page(url, …)` ties them together: stream → `feed_bytes` → first-paint at the head-complete checkpoint → finished `Page`; the shell `render` uses it and prints the first-paint height. **Verified live:** `fetch_streaming` over example.com (h2) reassembles byte-identical to the buffered fetch; the shell streaming-renders example.com with a first paint at the checkpoint. Streaming assumes UTF-8 (streaming transcode for legacy charsets is the remaining follow-on); the preload scanner + speculative preconnect stay deferred. **Speculative preconnect landed (2026-07-10):** `manuk_net::Preconnector` warms a link's origin on a user gesture — a **TCP+TLS handshake only, no HTTP request** (`warm_origin` drives the shared connector's `Service<Uri>` then drops the socket; because the connector is now a shared `OnceLock` cloned into both the pooled client and the preconnector, the handshake populates the *same* `rustls` session cache the real navigation resumes from, saving the TLS round-trips). **Privacy (Axis F): user-initiated + same-origin ONLY** — `classify()` refuses cross-origin/other-scheme/other-port targets (`DeclinedCrossOrigin`) so hover intent never leaks to a third party; bounded by a 6-in-flight cap + a 10 s per-origin idle window. Tested: pure same-origin policy (no network) + a live warm (`Warmed` → `SkippedRecent` → `DeclinedCrossOrigin`). **Remaining:** the GUI hover-hit-test signal (winit pointer over a link rect → `Preconnector::preconnect`) — the gui-feature integration point; the request-free warm + policy + budget are done. Page-declared `<link rel=preconnect>` (solicited) is a separate D4-scheduler path. The preload scanner stays deferred.
- **B2 connection reuse / preconnect / DNS-prefetch.** *Finding:* `hyper_util::client::legacy::Client` already provides a keep-alive connection **pool** (default 90s idle), and its `HttpConnector` does **Happy Eyeballs** (RFC 6555/8305, 300ms default), threaded DNS, and `SO_KEEPALIVE` (docs.rs/hyper-util). Chrome reaps idle *preconnected* sockets at ~10s and warns that over-preconnecting saturates the pool; preconnect leaks navigation intent (DNS+TCP+TLS) so it must be user-initiated and same-origin-biased, with `dns-prefetch` as the cheaper maybe-origin fallback (developer.chrome.com/Lighthouse, web.dev/resource-hints, MDN). *Disposition:* **reuse** hyper-util `legacy::Client` + `HttpConnector` for pooling/happy-eyeballs/DNS, wrapped with a rustls TLS connector (**reuse** `hyper-rustls`, small dep, or a tiny custom `tower::Service`) — replaces the current per-request `conn::http1::handshake`; this is also compatible with B1's streaming body (`Incoming`). **Hand-roll** the speculative preconnect trigger in the shell (hover/pointerdown → warm a pooled socket, ~10s idle cap, bounded concurrency), gated user-initiated; **defer** it until the pooled client + a shell hover signal exist. *Privacy constraint:* no unsolicited cross-origin preconnect without policy. *Roadmap:* resolve the §1 "connection reuse/pooling" and "speculative preconnect/DNS-prefetch" clauses to the above. *Pass-2 note (2026-07): the connector question is closed under **B1's pass-2 resolution** — use `hyper-rustls::HttpsConnector` (ALPN `h2,http/1.1`) with `hyper_util::client::legacy::Client`; one stack unifies pooling (B2) + h2 (B3) + streamable body (B1). Prefer `hyper-rustls` over a custom `tower::Service`.* No WPT target.
- **C1 realize hibernation.** *Finding:* Chromium splits it into two mechanisms that map onto our tiers — **freeze** (Energy Saver: after a tab is hidden+silent ~5min, stop JS/CPU and throttle DOM timers to ~1/min via "intensive wake-up throttling"; **keeps full RAM**; dispatches `freeze`/`resume`; audio/WebSocket/RTC tabs exempt) and **discard** (Memory Saver: unload the tab — resident drops from ~80–300MB active to ~5–10MB metadata/favicon, reloads on activation; this is the real RAM reclaim) (chromium.org tab-discarding, developer.chrome.com freezing/energy-saver). SpiderMonkey uses **compartments** for per-tab isolation (Firefox's own model) and **realms** for globals, loading self-hosted code once from a single compressed stream at init — there is **no V8-style deployable startup snapshot** (firefox-source-docs, spidermonkey.dev). *Disposition:* **hand-roll** the freeze/discard actions in the compositor (tiers exist, actions don't): background-CPU tier = *freeze* (throttle timers/JS task queue, keep DOM+layout, shrinking GC), hibernated tier = *discard* (drop fragment tree + rasterized tiles + the tab's realm, retain URL/DOM-source, relayout/re-fetch on wake — where RAM is actually reclaimed). **Reuse** SpiderMonkey compartment/realm-per-tab over the shared process-global runtime for low per-isolate baseline. *Reframe:* the §1 phrase "share SpiderMonkey startup snapshots across isolates" is inaccurate — replace with "compartment/realm-per-tab over one process-global runtime (SpiderMonkey has no V8 snapshot; self-hosted code loads once at init)." *Prereq:* per-tab baseline-RSS measurement (§8) to prove the freeze/discard deltas. No WPT target. **Realized (2026-07-10):** the two actions + per-tab accounting are wired. **Compositor `TabManager`** gained per-tab memory accounting (`set_mem`/`mem`/`total_mem` — the eviction-budget figure). **Shell `tab::Browser`** (where the heavy `Page` lives) realizes the actions tied to each tab's tier via `apply_tiers()`: focused → live+unfrozen; background-CPU → **freeze** (keep the `Page`, set a `frozen` flag the event-loop driver consults to throttle that tab's `setTimeout` queue ≤1/min); hibernated → **discard** (drop the boxed `Page` = fragment tree + computed styles + parsed DOM, retaining only URL + source HTML). **wake** re-lays-out from the retained source (a deeper reclaim would drop the source too and re-fetch from URL). `Page::estimated_bytes()` is the retained-heap proxy. **Verified:** `discard_reclaims_memory_and_wake_restores` — with `max_background=1` over 3 loaded tabs, the hibernated tab is discarded and its retained bytes drop >3× vs a live tab (freeze flag set on the background tab, discarded flag on the hibernated), then wake restores the memory; compositor `per_tab_memory_accounting_totals`. **Honest limit:** the accounting is a **retained-heap estimate** (`estimated_bytes`), not a true process-RSS figure — RSS reclaim additionally depends on the allocator returning freed pages to the OS; a `/proc`-based RSS probe (§8 metric #3) is the remaining measurement wire-up. `browse`'s single-window UI keeps its own `Page` and doesn't yet drive the multi-tab C1 path (documented). **RSS probe wired (2026-07-10):** `manuk_compositor::mem::process_rss_bytes()` reads whole-process RSS from `/proc/self/status` `VmRSS` on Linux (pure `std`; macOS `getrusage`/Windows PSAPI need a platform crate → `None`, engineered-unverified). The shell `render` prints it; a headless example.com render measures **~60.6 MB** (mostly `fontdb`'s system fonts + process floor). This is the ground-truth counterpart to `TabManager::total_mem`'s per-tab heap estimate — tabs share one process so true per-tab RSS isn't directly separable (the G-e shared-process attribution wrinkle); C1 proves the retained *heap* drops on discard, RSS following depends on the allocator. Recorded in bench/BASELINE.md metric #3 (now WIRED for Linux).
- **A1 Vello GPU paint tier.** *Finding:* the tier crates exist — full `vello` (GPU compute) and the "sparse strips" `vello_cpu` / `vello_hybrid` — but as of early 2026 sparse strips is **0.0.7, experimental, no API-stability guarantee** (any release likely breaking). `vello_cpu` is the most mature (near-beta, fastest Rust CPU renderer, now has Gaussian blur / drop-shadow / flood, non-layer clipping, gradients, opaque-image fills); `vello_hybrid` (GPU) and full `vello` remain alpha with known rough edges (complex-paint glyphs, high-winding paths, WebGL-on-Adreno, conflation). Even **Servo adopted Vello only for the bounded `<canvas>` surface, keeping WebRender for page compositing** (linebender.org TMIL blogs; github.com/linebender/vello releases; doc.servo.org/vello_cpu). *Disposition:* **defer** the focused-tab GPU tier (`vello`/`vello_hybrid`) on API instability + alpha rough edges — the existing `Painter` trait isolates the swap, so waiting is cheap and correctness-first. **Keep `tiny-skia`** as the CPU tier now; **track `vello_cpu`** as the eventual *richer* CPU tier (adds blur/shadow/clip we lack) and **reuse** it at its first stable (≥0.1) release; if prototyping the GPU tier sooner, pin an exact 0.0.x. *Roadmap:* §1 "Frame timing + rendering quality" resolves to — `Painter` seam ready → adopt `vello_cpu` at first stable release → GPU `vello_hybrid` after API stability + glyph fixes; `tiny-skia` correctness-first meanwhile. *Pass-2 freshness (2026-Q1):* Vello CPU is now "broadly usable / ready for production use cases" **feature-wise**, but its **API is still explicitly not stable** (not ≥0.1); Vello Hybrid is "roughly beta" with no API-stability guarantee; sparse strips still 0.0.7 (linebender.org tmil-25). Disposition **unchanged (defer)**, trigger **sharpened**: the CPU tier's *features* are ready, so **API stability is now the sole blocker** — adopt `vello_cpu` behind the `Painter` seam the moment its API stabilizes. No WPT target (rendering quality → visual reftests, not WPT layout/DOM). **Frame-time instrument wired (2026-07-10, §8 metric #4):** `manuk_compositor::FrameTimer` — a rolling per-frame window with last/average/**p95**/FPS/**jank** (frames over `FRAME_BUDGET_60FPS` ≈16.67 ms). Display-agnostic: the headless `render` times the CPU raster around `Page::paint` (prints `frame: N ms`), and the shell `gui` redraw loop wraps `gpu.draw()` with `begin`/`end` to time real **GPU present** frames (logged per 120-frame window; compiles, unverified headlessly — no display here). **Measured (CPU raster):** `paint_frame_800x600` ≈ **675 µs**, a live example.com render ≈ 3.2 ms — both far under the 16.67 ms 60-fps budget, so the CPU raster tier is not the frame bottleneck for representative pages (recorded in bench/BASELINE.md metric #4, now WIRED for the CPU tier). **All four §8 metrics are now wired** (binary size / latency+first-paint / RSS / frame time) except GPU-present-under-a-display, which awaits a headful session (and the deferred Vello GPU tier).
- **A2 incremental / damage-driven relayout + repaint.** *Finding:* the established model, used across engines, is the **double dirty-bit** — a node dirty bit (`IS_DIRTY`) plus an ancestor **summary bit** (`HAS_DIRTY_DESCENDANTS`) so incremental layout skips any subtree whose summary bit is clear — combined with **RestyleDamage**: diff old vs new computed style into damage levels (Repaint vs Reflow vs box reconstruct), with forced reflow on viewport change (servo wiki Layout-Overview / Workweek-incremental-layout; developer.chrome.com BlinkNG; arXiv:2411.10659 "Spineless Traversal" as a newer refinement). **`taffy` supports incremental relayout**: a per-node cache + `mark_dirty()` invalidates a node and its ancestors so unchanged subtrees reuse cached results — it is *not* full-recompute-only; Servo is actively reusing taffy boxes incrementally (servo/servo PR #37751). *Disposition:* **reuse the algorithms, hand-roll the engine glue** (they're algorithms, not crates): add per-node dirty + summary bits and a Servo-style RestyleDamage diff over our `ComputedStyle`, feeding the compositor's existing `Damage` for repaint; **reuse** taffy's `mark_dirty`+cache for flex/grid subtrees, hand-roll the equivalent cache for our block/inline fragment tree. **Defer, blocked on** dynamic mutation existing (D3 DOM mutation / scroll-hover restyle / resize) — the current one-shot static render has nothing to invalidate; but carry the dirty/damage bits in the fragment tree **now** so the incremental path is a fill-in, not a rewrite. *Pass-2 resolution (2026-07) — the three data structures, beyond the double-dirty-bit:* **(1) Invalidation (skip re-matching):** build a **RuleFeatureSet** at stylesheet-parse time mapping each key (class / id / attribute) → **descendant vs sibling `InvalidationSet`s**; a runtime class/id/attr change looks up only those keys and marks matching descendants/siblings dirty, with a **whole-subtree fallback** for complex selectors (chromium blink style_engine / RuleFeatureSet). **(2) Style sharing (skip re-computing):** a **matched-properties cache** (hash the matched declarations → reuse a cached `ComputedStyle`) + **sibling style sharing** (reuse a neighbor's computed style when inputs are identical), honoring the documented **sharing-breakers** (id, inline style, container units, registered event handlers) (webkit matched-declarations cache). **(3) Layout reuse (skip re-layout/rebuild):** a **`LayoutDamage` taxonomy** — box-tree *structural* rebuild → box rebuild *reusing structure* → *fragment-layout-only* ("empty" damage) → repaint-only — with **cached fragments on `LayoutBox`** invalidated precisely in the damage traversal; Servo's active 2025 incremental-layout PRs are the live reference (servo/servo #37751, #37957 "skip box tree construction", #38084 inline, #37866/#37868 atomics/floats). *Disposition:* **reuse all three algorithms, hand-roll the engine glue** over our arena DOM + from-scratch block/inline fragment tree (RuleFeatureSet in `manuk-css`, matched-properties/sharing cache in the cascade, `LayoutDamage` enum + cached fragments on `LayoutBox` → the compositor `Damage`). Still **deferred, blocked on D3 dynamic mutation**, but these are the structures to seed now. *Roadmap:* §1 "Incrementality" resolves to the double-dirty-bit + RestyleDamage → `Damage` design above, plus the RuleFeatureSet / style-sharing / LayoutDamage structures here. No new WPT target (correctness reuses the same layout reftests, run incrementally vs full). **Seeded + first activation landed (2026-07):** **(A2a)** the **double dirty-bit** is in `manuk-dom` — per-node `dirty` + `dirty_descendants` summary; `mark_dirty` propagates the summary up the ancestor chain (stopping at the first ancestor already carrying it); wired into `set_attr`/`append_child`/`detach` (+ a `structure_changed` flag for structural mutations); API `is_dirty`/`has_dirty_descendants`/`subtree_clean`/`clear_dirty`/`clear_all_dirty`. And **RestyleDamage** `{None<Repaint<Reflow<Rebuild}` + `diff_style(old,new)` is in `manuk-css` (display→Rebuild, geometry→Reflow, paint-only→Repaint). **(A2b)** `Page::relayout_incremental` activates them: a clean tree (or a mutation that yields no style/structure change) → `None` (zero work); otherwise re-cascade, classify the tree's damage, and relayout — returning the damage so the compositor can drive repaint-region vs reflow. `Page::load`/`relayout` clear the bits after a clean pass. Tested: unmutated→None, background→Repaint, height→Reflow(+height grows), append→≥Reflow, settles to None. **Deferred fill-ins (documented in the method):** subtree-partial reuse (skip clean subtrees via the summary bit — needs cached fragments + input-width tracking on `LayoutBox`); a paint-only refresh that skips layout (needs paint attrs read live, not baked into the fragment tree); RuleFeatureSet-driven partial restyle; matched-properties/sibling style-sharing cache.
- **A3 shaped-run + decoded-image caching.** *Finding:* Firefox/Blink both cache shaped runs; the standard granularity is **word-level** (split on word boundaries) for hit rate, with the known caveat that per-word shaping breaks OpenType **contextual** features needing cross-word context — so word-level needs a run-level fallback for such scripts/features (bugzilla 761442; harfbuzz shape-plans keyed on features). Cache key components: **font identity + size + run text + script/direction/lang + features**. Decoded-image caches use **LRU + a byte budget over discardable memory** (freeable under memory pressure; Chromium `cc/tiles/image_decode_cache.h` + Skia discardable, timestamp-LRU eviction). *Disposition:* **hand-roll the keys + per-tab accounting, reuse `lru` for eviction** (LRU is a tiny solved crate; keys/accounting are engine-specific). (1) **Shaped-run cache in `manuk-text`** extending the existing `FontKey` font cache — key `(FontKey, quantized size, text, script/dir/features)`, word-level with run-level fallback (safe now: our shaping is Latin/no-features), `lru` + byte budget — **buildable now**, a low-risk win especially once A2 re-shapes on relayout. (2) **Decoded-image cache** keyed `(resource-id/URL, decode size, format)` → RGBA, LRU + byte budget — **defer, blocked on** image loading/decoding (D4). *Cross-ref C1/§1 per-tab memory:* account both caches **per tab** and wire to the compositor budget so a discarded/hibernated tab drops them. (HTTP cache is separate — net/loading.) *Roadmap:* §1 "Caching" resolves to shaped-run-now + decoded-image-on-D4, both per-tab-accounted and evicted on C1 discard. No WPT target. **Shaped-run cache landed (2026-07-10):** `manuk-text` `FontContext::measure` is now backed by a bounded **`lru::LruCache`** keyed `(FontKey, quantized size bits, run text)` → advance width (cap 8192 entries), with `measure_cache_stats() -> (hits, misses)` counters. Layout measures the same words repeatedly (per line + shrink-to-fit's multiple passes + relayout), so repeats are LRU hits that skip the per-glyph metrics. Tested: `measure_cache_hits_on_repeat` (repeat → hit, different size → distinct key) and `shaped_run_cache_hits_during_layout` (a repeated-content page hits ≫ misses in real layout). Honest perf note: on fully-*diverse* text the win is ~neutral (the tuple-key `String` construction offsets the saved metrics, and parse/cascade dominate the pipeline) — the win concentrates on repeated runs, tables/shrink-to-fit multi-pass, and resize relayout. **Word-level + script/dir/features keying and a byte budget** (vs the current entry-count cap) are refinements; safe to omit now since shaping is Latin/no-features. **Part 2 (decoded-image cache) still deferred** — blocked on image *loading+decoding* (D4 fetches `<img>` bytes but rendering/decoding is a follow-on). Per-tab accounting + C1-discard eviction land with C1.

### 2. Web-traversal versatility (compatibility breadth)
Broadest real-world site set (excluding the hostile/bot-walled tier), WPT-verified.
- **Layout:** floats, tables, absolute/relative/fixed/sticky positioning, margin
  collapsing, CSS grid (via `taffy`), overflow/scroll containers, writing-modes, bidi.
- **CSS depth:** wire **Stylo**'s real cascade behind the `StyleEngine` adapter;
  media queries, `@font-face`, the full selector grammar, custom properties,
  `calc()`, transforms, transitions/animations, gradients.
- **JS + DOM:** grow the Web API bindings (`engine/js::bindings`) projecting the DOM
  into SpiderMonkey, WebIDL-driven, WPT-verified per API; event loop + timers;
  `fetch`/`XHR`; events; DOM mutation.
- **Loading:** external stylesheets/scripts/images (*the large remaining D4 part*);
  ✅ gzip/br/deflate decoding (P0.4); ✅ charset sniffing (`encoding_rs`+`chardetng`,
  WHATWG order — D4 pt.1); ✅ `data:` URLs (D4 pt.1); `blob:` on D3.
- **Protocols:** ✅ **HTTP/2 active** (ALPN, P0.4) + gzip/br/deflate decode; add **HTTP/3**
  (`quinn`), WebSocket (`tokio-tungstenite`), cookies.
- **Forms + navigation:** form submission, history (back/forward), fragment nav.

**Research dispositions (2026-07):**
- **B3 HTTP/2, HTTP/3, compression.** *Finding:* **h2** negotiates over TLS ALPN — advertising `h2` in the rustls connector lets hyper/hyper-util pick it automatically; multiplexing's win is real only with many same-origin **subresources**, so it is gated on external-resource loading (D4) — top-level-document-only fetch gains little over h1 keep-alive (B2). **h3** = `quinn` + hyperium `h3`/`h3-quinn` (h3-quinn still 0.0.x); browsers discover h3 via **Alt-Svc**/HTTPS-RR then **fall back to h2** if UDP/certs fail — so it needs an initial h1/h2 connection, Alt-Svc caching, a UDP path, and fallback logic (crates.io/h3-quinn, RFC 9114). **Compression**: `async-compression` exposes feature-gated tokio `AsyncRead` decoders for gzip/deflate/br/zstd (wrapping miniz_oxide/brotli), composing with B1's streaming body (github.com/Nullus157/async-compression). *Disposition:* **reuse** — (1) flip ALPN to `h2,http/1.1` once B2's pooled client lands, **defer the payoff** to D4; (2) **reuse** `quinn`+`h3` but **defer** h3 behind Alt-Svc discovery + h2 fallback (marginal win, pre-1.0); (3) **reuse** `async-compression` for `Content-Encoding` decode now (send `Accept-Encoding: gzip, deflate, br`), higher priority than h3 since real sites gzip/br regardless — brotli's static-dictionary size is tracked under C3. *Roadmap:* §2 "Protocols" resolves to h2-first (ALPN flip, D4-gated payoff) then deferred h3; add compression to §2 "Loading". No WPT target.
- **D1 layout: floats, tables, positioning, margin collapsing.** *Finding:* per-feature WPT targets exist as concrete subdirs — floats `css/CSS2/floats/` + `css/CSS2/floats-clear/`; margin collapse `css/CSS2/margin-collapse/` (spec: CSS2 §8 box model); positioning `css/CSS2/abspos/` + `css/CSS2/positioning/` + `css/CSS2/zindex/` and sticky `css/css-position/`; tables `css/css-tables/` + `css/CSS2/tables/` (spec: CSS2 §17). Servo's algorithms are the reference: floats via a sequential `FloatContext`/`PlacementAmongFloats` pass (parallelism disabled inside a float-containing BFC, an `Option<&mut FloatContext>` threaded through); tables in `components/layout_2020/table/layout.rs` with fixed vs auto algorithms (fixed triggers except when `inline-size:auto`, per CSSWG). Servo confirms CLAUDE's warning: "most layout bugs are float × block-size × line-breaking × margin-collapse interactions" — these are entangled, and table auto-layout is the most spec-ambiguous (servo wiki Layout-Overview; servo `table/layout.rs`; PRs #35170/#35882/#33098; w3.org/TR/CSS2 §8/§17). *Disposition:* **hand-roll each** — from-scratch layout is the mandate (taffy only covers flex/grid, no crate crosses this boundary). Sequence by entanglement: (1) **floats + margin-collapse together** as one BFC-aware pass (the #1 bug area), (2) **positioning** via a containing-block chain (abs/rel/fixed, then sticky), (3) **tables** fixed→auto, **documenting the chosen interpretation** where the spec is ambiguous (working-agreement requirement). *New WPT targets:* the subdirs above; current pass count 0 (unimplemented) → target = pass each subdir's applicable reftests; **exact current-vs-target counts require the upstream WPT runner** (C-axis §8) — not yet stood up. *Roadmap:* attach these targets + sequencing to the §2 "Layout" bullet. **Progress (2026-07):** **sub-feature (1) landed** — CSS plumbing (`float`/`clear`/`position`/insets/`z-index`, D1a); **adjacent-sibling margin collapsing** (`collapse_margins`, D1b); and a **BFC-aware `FloatContext`** (D1c) doing left/right float placement, horizontal stacking + wrap-to-next-band, clearance, shrink-to-fit (`min(max-content, avail)`), and **text flow-around** (per-line float-band query in inline layout, floats re-origined via `LayoutBox::translate`). Verified by 7 layout unit tests demonstrating each behavior; the P0.3 reftest runner still gives 14/3/14 on `css/CSS2/abspos` (no regression). **WPT-reftest acceptance for `css/CSS2/floats/` is pending a full WPT checkout at the pin** — the local `/tmp/wpt` is a partial (abspos-only) tree; `floats/`, `floats-clear/`, and `margin-collapse/` are not present there (and `margin-collapse/` was already noted empty at the pinned SHA). Documented simplifications: parent↔child margin collapse remains. **Sub-feature (2) positioning landed (2026-07):** `position:relative` (visual shift via `LayoutBox::translate`, flow unaffected — `BlockResult.flow_bottom`); `absolute`/`fixed` via a final `position_absolutes` pass resolving each box against its containing block (nearest positioned ancestor's padding box for abs, viewport for fixed / initial CB) with left/right/top/bottom insets, stretch-between-insets and shrink-to-fit widths, and nested-abs support (DOM pre-order + running rect map). Verified by 4 positioning unit tests; `css/CSS2/abspos` holds at **14/3/14** (no regression — inset-less *static-position* abs boxes are left unplaced, documented, pending static-position tracking + inline-block boxes). `sticky` and `z-index` ordering still pending. **Sub-feature (3) tables landed (2026-07):** `display:table`/`table-row-group`/`table-row`/`table-cell` (+ UA defaults for `<table>`/`<tr>`/`<td>`/`<th>`/`<thead>`…), a dedicated `layout_table` that gathers rows (flattening row groups) → per-column intrinsic min/max widths → **fixed** (first-row/explicit widths, autos split remainder) or **auto** (min/max distribution, shrink-to-fit when table width is auto) column sizing → cells laid out as BFCs and stretched to row height → separated-borders model via `border-spacing`. Verified by 4 layout + 2 css unit tests **and an end-to-end PNG render** (2×3 colored table, correct column widths, visible spacing, per-cell text). Documented interpretations (CSS2 §17 ambiguity): no `colspan`/`rowspan`, no `border-collapse`, captions/`<col>` hints/relpos-on-table ignored, minimal anonymous-box fixup. **D1 COMPLETE** (all three sub-features); WPT-reftest acceptance for `css/CSS2/tables/`+`css/css-tables/` still pending a full WPT checkout at the pin.
- **D2 Stylo real-cascade activation.** *Finding:* Stylo **is** embeddable outside Servo — **Blitz** (`blitz-dom`) does it with *nearly our exact stack* (Stylo for CSS + Taffy for layout + Parley for text), using a `stylo_taffy` glue crate to bridge Stylo's output to layout. The embedder must implement Stylo's DOM trait "wall" — **`TDocument`, `TElement`, `TNode`, `TRestyleDamage`** (plus `selectors::Element` for matching) — over its DOM, provide a **`Device`** (viewport/media state), build a **`Stylist`** (compiles selectors into `SelectorMaps` keyed by origin/pseudo, holds the `Device`), run cascade → **`ComputedValues`**, and map those to our layout input (book.servo.org/style; doc.servo.org/style Stylist; github.com/servo/stylo; github.com/DioxusLabs/blitz). Build cost: large dep tree (stylo_dom/selectors/cssparser 0.37/servo_arc), ~20s here. *Disposition:* **reuse Stylo** behind the existing `StyleEngine` adapter (it is the CLAUDE target and unlocks the full cascade — media queries, `@font-face`, full selector grammar, custom properties, `calc()` — for free); Blitz de-risks it as a working reference of our exact stack. The cost is **bounded, well-scoped glue**, not a research risk. *One impedance to resolve in implementation:* Stylo stores per-element `ElementData` on the element; our **arena** `manuk-dom` (`NodeId` indices) has no pointer-node with attached style storage, so we need a parallel per-node style store or a `TElement` wrapper over `(Dom, NodeId)` — mirror how `blitz-dom` attaches Stylo data. *Pass-2 (2026-07) — impedance resolved:* Stylo's per-node style state is **`AtomicRefCell<ElementData>`** (Servo names it `ServoNodeData`; `TElement::borrow_data()`/`mutate_data()` return it; `ElementData` holds the computed `ElementStyles`), using **`atomic_refcell`** for thread-safe runtime-checked borrows because Stylo restyles in parallel (doc.servo.org atomic_refcell; servo/stylo). *Resolution:* attach an **`AtomicRefCell<ElementData>` per element node** — either a field on the arena element `Node` or a `NodeId`-indexed side-table (avoids bloating text/comment nodes) — and impl `TElement`/`TNode` for a lightweight `(&Dom, NodeId)` handle whose `borrow_data`/`mutate_data` return it and whose navigation reads the arena links. This is exactly `blitz-dom`'s pattern (atomic_refcell + Stylo); the impedance is closed — no adoption of Servo's heavier machinery required. *New WPT targets (post-activation):* `css/selectors/`, `css/css-cascade/`, `css/mediaqueries/`, `css/css-variables/`, `css/css-values/` (calc); current 0 → target = Stylo's Firefox-grade coverage; exact counts need the upstream runner. *Roadmap:* resolve the §2 "CSS depth" bullet to the above (reuse + trait-wall glue + arena `ElementData` store). **Step-0 outcome (2026-07, `engine/css::stylo_probe`, feature-gated):** the *non-DOM half* of the pipeline is **proven working here** — a probe builds a servo `Device` (`Device::new` + a 2-method `FontMetricsProvider` stub + `ComputedValues::initial_values_with_font_override`), parses a real author stylesheet with **Stylo's own parser** (`Stylesheet::from_str`), feeds it to a `Stylist`, flushes into `CascadeData`, and reads back **3 selectors / 6 declarations** from `p{color:red} .lead{font-weight:bold} #x a{margin:4px}` — the 6 (not 3) confirms real longhand expansion (`margin`→4), i.e. Stylo's genuine property machinery, not a stub. Deps added under the `stylo` feature: `url` + `euclid` (versions matched to stylo 0.19); default build unaffected. **Trait-wall measured (the remaining, large half):** `TElement` 76 + `TNode` 20 + `selectors::Element` 30 = **126+ methods** over interlocking associated types (`ConcreteNode`, `TraversalChildrenIterator`, `TDocument`, `TShadowRoot`) + the `AtomicRefCell<ElementData>` store — Blitz-scale (~1.5–2k LOC). **Decision:** the reuse disposition holds and the risky unknowns (does Stylo build/parse/cascade-compile here? does `Device`/`Stylist` construct without Servo's layout crate?) are now **closed** — what remains is the *bounded-but-large* trait-wall glue, correctly scoped as a **dedicated multi-session effort** (not attempted in one sitting, since a half-written 126-method wall leaves the crate uncompilable). The `StyloEngine` adapter keeps delegating to `MinimalCascade` until the wall lands, so callers see no half-state. *Next when resumed:* impl `TNode`/`TElement`/`selectors::Element` for a `(&Dom, NodeId)` handle + the `NodeId`-indexed `ElementData` side-table, then map `ComputedValues`→`ComputedStyle`. **Pass-3 landed (2026-07, `engine/css::stylo_dom`, feature-gated):** the **`ElementData` side-table + `StyloElement` handle** are built (`ElementDataStore` = `NodeId`→`AtomicRefCell<ElementData>`, borrow/borrow_mut/ensure/clear; `StyloElement{dom,node,store}` navigates the arena + reaches its cell — test `element_data_attaches_per_node_with_borrow_semantics`), and the **first of the three trait walls — `selectors::Element` (30 methods) — is fully implemented and verified**: `type Impl = stylo::selector_parser::SelectorImpl` (Servo's interned-atom impl), our plain-`String` arena tag/id/class/attr values bridge to the atoms via deref-to-`str` at each comparison; navigation (`parent_element`/prev+next element sibling/`first_element_child`) walks the arena links; `has_local_name`/`is_same_type` compare tags, `has_id`/`has_class` honor `CaseSensitivity`, `attr_matches` uses `AttrSelectorOperation::eval_str`, `is_empty`/`is_root`/`is_link` are real, `opaque` = arena node address. **Proof it runs end-to-end (`stylo_selector_matcher_runs_over_arena_dom`):** Stylo's *production* matcher `selectors::matching::matches_selector`, fed selectors parsed by Servo's own `SelectorParser::parse_author_origin_no_namespace`, matches correctly over the arena DOM across type/id/class/multi-class/attr-exists/attr-equals + **descendant and child combinators** + `:empty` structural pseudo (16 assertions, positive + negative). Dep: `selectors = "0.39"` (unified single-version with stylo 0.19, verified via `cargo tree`) added under the `stylo` feature; default build unaffected; 16/16 stylo-featured tests green, clippy-clean. **Documented scope for this tranche:** pseudo-classes (`:hover`/`:focus`/`:link`…), shadow DOM, `::part`, `assigned_slot`, and custom-state return `false`/`None` (matcher-visible but not yet modelled). **Still remaining for the full cascade:** `TNode` (20) + `TElement` (76) walls + `ComputedValues`→`ComputedStyle` mapping; the `StyloEngine` adapter keeps delegating to `MinimalCascade` until those land. **Data-store upgraded to Stylo's real type (2026-07):** `ElementDataStore` now holds **`stylo::data::ElementDataWrapper`** per node (not a bare `AtomicRefCell<ElementData>`); its `borrow()`/`borrow_mut()` return Stylo's own **`ElementDataRef`/`ElementDataMut`** — the *exact* types `TElement::borrow_data`/`mutate_data`/`ensure_data` must return (those handles have private fields, constructible only through the wrapper, which derives `Default`). `has_data` added. This closes the single hardest-to-discover impedance of the `TElement` wall — our arena now produces Servo's genuine element-data borrow types (14 stylo tests green; direct `atomic_refcell` dep dropped since the wrapper encapsulates it). **Turnkey map for the remaining TNode/TElement/TDocument/TShadowRoot cluster (all signatures + type paths + construction unlocks now resolved — the next session is transcription, not research):** the four traits are **mutually recursive** (`TNode::{ConcreteElement: TElement, ConcreteDocument: TDocument, ConcreteShadowRoot: TShadowRoot}`) so they compile only as ONE indivisible unit (~1.5–2k LOC); a half-written wall leaves the crate uncompilable (why it is one dedicated commit). Concrete types to add: `StyloNode<'a>(dom,node,store)` (any node) as `ConcreteNode`; reuse `StyloElement` as `ConcreteElement` (already `selectors::Element`); `StyloDocument`/`StyloShadowRoot` (shadow = zero-variant/never-constructed is fine — no shadow DOM yet). Supertraits to also impl: `NodeInfo{is_element,is_text_node}`, `AttributeProvider{get_attr(&LocalName,&Namespace)->Option<String>}`, and `Eq+PartialEq+Hash+Copy` on the handles (derive/by-NodeId). Required-method construction paths (all verified against stylo 0.19 source `dom.rs`): navigation/`as_node`/`owner_doc`/`opaque`/`debug_id` from arena links; `is_html_element→true`, mathml/svg→false; `style_attribute→None`, `animation_rule`/`transition_rule→None`, `state()→ElementState::empty()`, `id()→` interned `WeakAtom` of the id attr, `local_name()→` a `web_atoms::LocalName` (intern our tag), `namespace()→` the HTML namespace atom; `each_class`/`each_attr_name`/`each_custom_state` iterate the arena element's classes/attrs (intern each to `AtomIdent`/`LocalName`); `borrow_data`/`mutate_data`/`ensure_data`/`has_data`/`clear_data` delegate to `ElementDataStore` (done above); dirty-descendant flags (`has_dirty_descendants`/`set_`/`unset_`) + `store_children_to_process`/`did_process_child` + `has_snapshot`/`handled_snapshot`/`set_handled_snapshot` → back with **per-node atomics** (`AtomicBool`/`AtomicIsize`) added to the side-table (we own it; the setters are `unsafe fn` but our storage makes them sound); `has_animations`/`may_have_animations`/`has_css_animations`/`has_css_transitions`/`needs_transitions_update`/`update_animations`→false/no-op; `shadow_root`/`containing_shadow`→None; `traversal_children→LayoutIterator(<arena element+text child iterator>)`; `query_container_size→Size2D::new(None,None)`; `match_element_lang`/`lang_attr`→simple/None. **External-crate deps the cluster pulls (must version-match stylo 0.19, add under the `stylo` feature):** `dom` (for `ElementState`), `app_units` (`Au`), `servo_arc` (`Arc`/`ArcBorrow`) — `selectors`/`euclid`/`smallvec` already present. Then a `TDocument` gives `shared_lock()→&SharedRwLock` (hold one in the store) + `quirks_mode()`; finally wire a sequential cascade driver (resolve `ComputedValues` per element via the `Stylist`) and map to `ComputedStyle`.
- **D3 Web API binding surface — PASS-2 SUBSTANTIALLY RESOLVED (fork narrowed to a lean hand-written subset over the arena DOM; only a one-interface prototype residual).** *Finding:* Servo's WebIDL codegen (`script_bindings/codegen/codegen.py` + Mozilla's ply-based `WebIDL.py`) generates Rust glue **tightly coupled to Servo's DOM object model** — `#[dom_struct]`, the `DomObject`/`Reflector` pattern (every DOM object = a Rust struct on the Rust heap **plus** a reflector `JSObject` in SpiderMonkey), GC-traced `Dom<T>`/`DomRoot`. Reusing the codegen means adopting that whole model, which **conflicts with our arena `manuk-dom`** (`NodeId` indices, no per-node reflector/GC root). Unlike D2, there is **no standalone reference** — Blitz runs no JS-DOM bindings, and no lightweight WebIDL→mozjs generator exists outside Servo/Gecko (book.servo.org, doc.servo.org/script/dom/bindings, github.com/servo/servo codegen). The empirical API-prioritization is *solved data*, though: public **Chrome UseCounter** stats (chromestatus.com/metrics/feature/popularity) give per-page feature usage — no need to instrument Chrome ourselves. *Why deferred (honest):* the decision is a real **architecture fork** that 3 searches can't settle — (A) adopt Servo's Reflector/GC DOM model + reuse its codegen (high fidelity, but replaces the arena DOM's role for scripted nodes and pulls Servo-coupled machinery) vs (B) keep the lean arena DOM and **hand-write / thin-codegen** bindings for a UseCounter-prioritized subset on mozjs `JSClass`/`JSNative` (more manual per API). Which wins depends on whether the arena DOM can host reflectors/GC roots acceptably, the size of the must-have subset, and the event-loop/microtask design — all requiring a **prototype**, not a lookup. *Unproven lean:* (B), to preserve the lean arena DOM — validate, don't assume. *Follow-up pass needs:* pull top DOM APIs for the target-site list from chromestatus UseCounter; prototype ONE interface (e.g. `Element.textContent`/`document.querySelector`) both ways to measure glue cost + arena-DOM/reflector fit; then commit the DOM object model. *Pass-2 resolution (2026-07) — the fork is substantially closed:* **(a) The arena DOM is NOT forced onto Servo's heavy object model.** SpiderMonkey exposes a native object via a **`JSClass` with reserved slots**; the reflector stores our **`NodeId` as a `JS::PrivateValue` in a reserved slot** (the *same* mechanism Servo uses for its Rust DOM pointers) — GC tracing is only needed for the *few* JS references a DOM node actually holds (event listeners, expandos), managed with `JS::Heap<T>` + a rooting side-table. gjs/cjs (GNOME) already hand-expose native objects to SpiderMonkey **outside** Servo (mozilla-spidermonkey embedding-examples GC-Rooting; servo/servo Servo-and-SpiderMonkey-Report). **(b) Reuse-vs-hand-write:** the storage mechanism is identical either way, so the fork is codegen-scale + GC-lifetime model, not the mechanism — **lean = (B) hand-write a prioritized subset** (gjs-style, `NodeId` in a reserved slot) preserving the lean arena DOM, with Servo's `script_bindings` codegen remaining an option as its "bring your own JS engine" modularization matures (servo/servo#43180). **(c) Event loop:** **reuse mozjs's `RustJobQueue`** for promise microtasks (it drives the host queue via `JobQueueTraps`) + **hand-roll a tokio-backed host task queue** for timers/events (`setTimeout` is host-provided, not engine) → the standard HTML macrotask→drain-microtasks loop (spidermonkey-embedding-examples#20; servo#38265/#38232). **(d) Prioritization:** chromestatus **UseCounter** + HTTP Archive **BigQuery** is the method; since **jQuery is on ~74% of pages**, the empirically-justified first tranche is the **jQuery-core surface** — `querySelector`/`getElementById`, `createElement`/`appendChild`/`textContent`/`innerHTML`, `addEventListener`, `XHR`/`fetch`, `classList`/`style` (almanac.httparchive.org/2024/javascript). *Disposition:* **hand-write the jQuery-core subset over the arena DOM** (reserved-slot `NodeId` + `JS::Heap` side-table for JS-refs) + reuse `RustJobQueue` + a host task queue. **Residual:** prototype ONE interface (e.g. `Element.textContent`) to lock in hand-write-vs-codegen before scaling — but the architecture fork is otherwise closed. *WPT targets (post-first-interface):* `dom/`, `html/dom/`, per-API suites. *Roadmap:* update the §2 "JS + DOM" bullet to the hand-written-subset-over-arena-DOM plan above. **Step-0 outcome (2026-07, DECIDED — `engine/js::bindings_prototype`, feature-gated):** a working prototype exposes `textContentOf(element)` where the `Element` reflector stores `(NodeId, *const Dom)` in **reserved slots**; JS calls it and gets `dom.text_content(node)` back — **validated end-to-end** (`cargo test -p manuk-js --features spidermonkey bindings_prototype -- --ignored` passes). *Confirms the arena `manuk-dom` needs NO Servo `Dom<T>`/Reflector machinery.* **Glue cost:** ~157 LOC for the first interface (mostly one-time boilerplate: JSClass + global + **realm entry** + native ABI + string handling); ~15–25 LOC per additional read-property. **Hazard surfaced:** a missing `JSAutoRealm` *compiled fine but SIGSEGV'd at runtime* — raw jsapi per-interface is realm/rooting-error-prone. **Decision:** proceed with **hand-write (option B)** preserving the arena DOM, **but on a thin safe binding-helper layer built once** (reflector creation, reserved-slot accessors, native-fn + realm/rooting wrappers), not raw jsapi per interface; Servo codegen stays the fallback if the subset balloons. **Production note:** use ONE process-global `Runtime` with realms/compartments per tab (the C1/§7 model) — SpiderMonkey doesn't cleanly support multiple `Runtime`s/process, which is why the prototype test runs isolated. **Tranche-1 landed (2026-07, `engine/js::dom_bindings`, feature-gated):** the thin safe helper layer is built and the first faithful jQuery-core methods run over the arena DOM — **`document.getElementById` / `querySelector` / `createElement`, `element.appendChild` / `setAttribute` / `getAttribute` / `querySelector`**. The helper layer absorbs the Step-0 hazards: reflectors of one shared `NODE_CLASS` carry `(NodeId, *mut Dom)` in reserved slots; `this` is read from `vp[1]` (no `CallArgs`); `JSContext::from_ptr` rebuilds the ergonomic `&mut JSContext` inside natives so the crate's `wrappers2`/`conversions` idiom applies (strings via `safe_from_jsval`/`to_jsval`); every slot read fails safe to `null`/`undefined`. `querySelector` reuses a new public `manuk_css::query_selector`/`query_selector_all`/`matches_selector` (the cascade's own engine, so JS and cascade agree). **Validated end-to-end** (isolated, one `Runtime`): a JS script does query+create+append+attribute round-trips and **mutates the arena DOM** (`cargo test -p manuk-js --features spidermonkey dom_bindings -- --ignored`; test passes, then the same benign SpiderMonkey process-exit teardown SIGSEGV as the prototype). Default build unaffected. **Mutation model:** the `*mut Dom` in a slot is deref'd `unsafe`ly — sound single-threaded for the reflectors' lifetime (documented). **Tranche-2 landed (2026-07):** accessor **properties** via `JS_DefineProperty1` (native getter/setter) — **`element.textContent` (get + set, setter replaces children with a text node → real arena mutation), `tagName` (read-only, uppercased), `id` (get + set), `className` (get + set)** — plus **`querySelectorAll`** returning a real JS `Array` NodeList (`NewArrayObject1` + `JS_SetElement1`, `Array.isArray`/`.length`/indexing all work). Validated in the same isolated end-to-end test (a JS script sets `textContent`/`id`/`className`, reads `tagName`, and iterates the NodeList; the arena DOM's text is asserted from Rust). **Tranche-3 landed (2026-07): `element.innerHTML` (get + set).** New `manuk_html::serialize_inner` (children→HTML, void-element + entity-escape aware) backs the getter; `manuk_html::set_inner_html` (parse the fragment as a document, deep-clone its `<body>` children across arenas via `clone_into`) backs the setter — so `el.innerHTML = "<em>hi</em>"` re-parses into the arena DOM and `el.innerHTML` reads it back, round-tripping. Validated in the same test (`parent.innerHTML = "<em>hi</em><i>yo</i>"` → `querySelectorAll("em").length === 1`, `textContent === "hiyo"`, exact re-serialization). Documented limits: pragmatic fragment parse (body-context only, no `<tr>`-in-table context), not an HTML-serialization conformance target. **Events tranche — the event-loop core landed (2026-07, `engine/js::event_loop`, feature-gated):** the HTML *run-a-task-then-drain-microtasks* loop, host-side over SpiderMonkey. `setTimeout(fn)` pushes a rooted global `__tasks` FIFO (macrotasks); `queueMicrotask(fn)` pushes `__micro` (microtasks), drained **fully** at each checkpoint (a microtask may enqueue more). `run()` = drain-microtasks → run one macrotask → repeat → final drain; both queues driven via `evaluate_script` so callbacks stay rooted in the global arrays (no Rust-side rooting hazard). Validated end-to-end (isolated): `queueMicrotask` + nested `setTimeout` produce the exact spec ordering `pTmU` (microtask before macrotask, nested microtask after its macrotask, FIFO macrotasks). **Honest environment blocker (flagged):** native `Promise.prototype.then` reactions are NOT yet routed through the loop — that needs SpiderMonkey's job queue (`JS::UseInternalJobQueues` + `RunJobs`), whose **mozjs-0.18 wrapper segfaults on call in this build** (no mozjs test/example exercises it; consistently crashes before returning). `queueMicrotask` (a first-class web API) covers host-scheduled microtasks meanwhile; Promise-queue integration is the tracked follow-up (retry on a newer mozjs, or implement a custom `JobQueueTraps`). **`addEventListener`/`dispatchEvent` landed (2026-07, `engine/js::dom_bindings`):** element methods over a JS-side listener registry (`__listeners`, keyed by `"<nodeId>:<type>"`, GC-rooted via the global). `addEventListener(type, fn)` stashes the handler on the global then a helper appends it; `dispatchEvent(type)` synchronously invokes the node's listeners with an `{type}` event object and returns whether any ran (simplified: a type string, not an `Event` object — no `Event` constructor yet). The natives use `CurrentGlobalOrNull` + `JS_SetProperty` + a tiny `evaluate_script` (embedding the arena `NodeId` + escaped type), so no `Call`/`HandleValueArray` plumbing. Validated in the `dom_bindings` test: a listener added from JS fires on synchronous dispatch (returns true; unknown type returns false) **and** on a dispatch scheduled through the event loop (`setTimeout(() => btn.dispatchEvent("click"))` → the click count reaches 2 only after `event_loop::run`). **`fetch`/XHR landed (2026-07, `engine/js::event_loop`):** the event loop is now a real **I/O loop**. `fetch(url[,{method}])` returns a host *thenable* (`.then(onF)` → Response-like `{status, ok, body, text()}`) and `XMLHttpRequest` (`open`/`send`/`onload`/`status`/`responseText`) is callback-based — both sidestep the blocked native-Promise queue. Neither touches the network in the JS native: each enqueues a pending request (`"id\x01kind\x01method\x01url"`) on `__pendingFetches`; the Rust loop [`run_with_fetcher`] performs the I/O via an **injected fetcher closure** `fn(method,url)->(status,body)` (production wires a `manuk-net`-backed closure at the call site — keeping `manuk-js` decoupled from the network stack; tests inject a deterministic mock), then delivers each result back into JS (`__deliverFetch`/`__deliverXhr`), which fires the thenable/`onload`. Loop turn: drain microtasks → perform all pending I/O (+deliver) → drain microtasks → run one macrotask; repeats while any work remains. Validated (isolated): `fetch("…/api").then(r=>…)` receives `200:hello:true` and an XHR `POST` `onload` receives `201:world`, both delivered through the loop via a mock fetcher. **Remaining D3/JS work:** an `Event` object/constructor + capture/bubble propagation; response streaming + real headers on `fetch`; native-`Promise`-queue integration (blocked on the crashing mozjs wrapper, above); production sugar (per-interface prototype objects). **This closes the D3 jQuery-core surface** (DOM read/mutate/query/serialize + events + event loop + fetch/XHR) modulo the documented follow-ups.
- **D4 loading: external resources, decoding, charset.** *Finding:* **charset** is a definitive reuse — `encoding_rs` is the WHATWG Encoding Standard implementation in Rust, used in **Gecko since Firefox 56**, and `chardetng` is Firefox's fallback legacy-encoding *detector* (small footprint when already depending on encoding_rs). The WHATWG sniff order is BOM (peek 3 bytes) → HTTP `Content-Type` charset → `<meta>` **prescan of the first 1024 bytes** (Gecko implements exactly) → `chardetng` → default (encoding.spec.whatwg.org; hsivonen.fi/encoding_rs, /chardetng). **Compression** (gzip/br/deflate) is already dispositioned in **B3** (`async-compression` on the streaming body). **Resource scheduling** is WHATWG HTML: CSS render-blocking, scripts sync-by-default vs `defer`/`async`, with the preload scanner (B1) overlapping fetch and parse. `data:` URLs are RFC 2397 (`data:[<mediatype>][;base64],<data>`); `blob:` needs a Blob object store. *Disposition:* **reuse `encoding_rs` (decode) + `chardetng` (fallback detect)**, **hand-roll** the small WHATWG sniff orchestration (BOM→Content-Type→1024-byte meta prescan→detector→default); **cross-ref B3** for content-decoding (decode precedes charset). **Hand-roll** the resource-fetch scheduler in `engine/page` (WHATWG-ordered, engine-specific), **blocked on** external-resource fetch existing (`<link>`/`<script src>`/`<img>`) + the pooled client (B2) + streaming parse (B1). **Hand-roll `data:`** now (trivial; we already have base64); **defer `blob:`** — blocked on D3 (Blob store needs the JS/DOM layer). *New WPT targets:* `encoding/` (charset — encoding_rs passes it) and `fetch/` (loading semantics); current 0 → target = encoding_rs/spec coverage; exact counts need the upstream runner. *Roadmap:* resolve §2 "Loading" to — fetch scheduling (hand-roll, B1/B2-gated) → decode (`async-compression`, B3) → charset (`encoding_rs`+`chardetng`+WHATWG sniff) → `data:` now / `blob:` on D3. **Part 2 landed (2026-07): external subresources.** `manuk-page` enumerates them in document order (`Page::subresources` → `Subresource{kind,url}`: `<link rel=stylesheet>` render-blocking, `<script src>` with `defer`/`async`, `<img src>`), resolving each href absolute against the page URL. **External stylesheets fetch + apply before paint:** `Page::apply_stylesheets(external_css, …)` combines inline `<style>` + external `<link>` sheets in document order (correct cascade precedence), re-cascades, and relayouts (returning the `RestyleDamage`); `Page::fetch_and_apply_stylesheets` does the `manuk-net` fetching, and the shell `render` command calls it so real pages pick up their external CSS. Tested: subresource enumeration (kinds + defer/async + resolved URLs) and an external sheet's `#box{height:250px}` applying → the page grows (Reflow). Script *execution* (JS path) and image *rendering* are the documented follow-ons; `blob:` still deferred.

### 3. Lightweightness (binary size + footprint)
- Verify + measure the **musl static** Linux and **static-CRT** Windows builds;
  report binary size before/after size-affecting changes.
- Strip unused SpiderMonkey features (WASM, debugger, unused ICU locale data) via
  build config — size wins that do **not** cross the JS modification boundary.
- Dependency hygiene: the default engine/agent graph is already lean (0 `wgpu`/
  `winit` — correctly gated behind `shell`'s `gui` feature); remaining duplicates
  are transitive (see C3). Prefer `default-features = false`; audit with
  `cargo tree --duplicates` / `cargo bloat`.
- Minimize per-tab baseline resident memory; measure it.

**Research dispositions (2026-07):**
- **C2 SpiderMonkey build-size reduction (config-only, inside the boundary).** *Finding:* the real lever is **ICU locale data**, not code — `--without-intl-api` drops `Intl`+ICU entirely, but ICU *data* dominates size and en-US-only data is ~3–4MB vs the full-locale set (bugzilla 1402379, firefox-source-docs/intl). Crucially, `mozjs` **links a prebuilt archive by default** (why it "builds" in seconds here) that is Servo's fixed config **with** Intl+ICU; custom configure flags only apply when `MOZJS_FROM_SOURCE=1` forces the ~30-min from-source build (github.com/servo/mozjs README). *Disposition:* **defer, blocked on** (a) a `MOZJS_FROM_SOURCE=1` build path + patched SpiderMonkey configure invocation, and (b) per-flag binary-size measurement (§8) — the default prebuilt hides the win and cannot be trimmed as-is. When unblocked: baseline `--enable-optimize --disable-debug`, **trim ICU locale data** (keep `Intl` — real sites use `Intl.*`; never `--without-intl-api`), treat **WASM disable** as a measured compat tradeoff (increasingly used → default keep). *Pass-2 (2026-07) — mechanism now concrete, only measurement remains:* `servo/mozjs` **hardcodes** the configure flags (no cargo/env knob for intl/ICU/wasm), so the path is: **`MOZJS_FROM_SOURCE=1` + patch `mozjs_sys`'s configure invocation**, then **bake a reusable trimmed archive once via `MOZJS_CREATE_ARCHIVE`** and link it thereafter via `MOZJS_ARCHIVE` (so the ~30-min build is one-time, not per-build) (github.com/servo/mozjs README). The **ICU win is locale-subsetting, keeping `Intl`** — Firefox ships **459 locales** (excessive); ICU **64+ has a data-filter/subset mechanism**, and Mozilla's own preferred cleanup is *"build ICU with just `en` data"* rather than `--without-intl-api` (which breaks real sites' `Intl.*`) — en-only ≈ 3–4MB vs the full set, plus ~50KB from dropping unused ICU features (confusables/stringprep/…); note `icudt*.dat` needs ICU CLI tools to (re)compile, so we supply a pre-filtered data file (bugzilla 1433325/1612379; firefox-source-docs/intl; ICU4X is the modular long-term direction). *Disposition (advanced):* **the research is now sufficient — the true residual is engineering, not research:** run the one-time `MOZJS_FROM_SOURCE` + patched-configure + en-only-ICU-data build under `MOZJS_CREATE_ARCHIVE`, **measure the size delta (§8)**, then ship the trimmed archive. Strictly build/config; never JIT/GC/sandbox. No WPT target. **Step-0 MEASURED (2026-07-10) — the gate number, from a real linked release binary:** SpiderMonkey adds **~32.9 MB** to the binary (JS-less baseline 5.1 MB → 38.0 MB with `--features spidermonkey`), of which the all-locales **`icu_data.o` is ~14.3 MB** (the trim target); `Intl.*` is functional in the build (`manuk eval 'new Intl.NumberFormat("en-US").format(…)'` evaluates). **Decision: adopt the en-only ICU trim** — est. **~12–13 MB saving (~1/3 of the binary)**, larger than the plan's single-digit-MB guess, clearly justifying the per-OS from-source bake. Realization stays engineering-gated (the ~30-min `MOZJS_FROM_SOURCE=1` + patched-configure + `--with-data-filter` build baked to `MOZJS_ARCHIVE`, per target); the measurement + decision are done and recorded in bench/BASELINE.md. Added a `spidermonkey` shell feature + `manuk eval <expr>` (real JS execution; also the link anchor for this measurement). No WPT target. **Realization blocked in this sandbox (2026-07-10, confirmed):** the from-source trim needs (a) a toolchain that isn't present — `clang`/`cbindgen`/`autoconf2.13` are all missing and there's no sudo to install them — and (b) a *source patch*, since `mozjs_sys`'s build.rs exposes only `MOZJS_FROM_SOURCE`/`MOZJS_ARCHIVE`/`MOZJS_CREATE_ARCHIVE` and **no intl/ICU/data-filter env knob** (servo/mozjs hardcodes the configure flags, as the research found). So the ~14.3 MB win is real and DECIDED but its bake is CI/per-OS infra work outside this environment (Linux/macOS/Windows each need the from-source build + patched configure + `--with-data-filter` baked to `MOZJS_ARCHIVE`). No further action possible here beyond the recorded measurement + decision. **LANDED (2026-07-10) — and the blocker above was WRONG on two counts.** (1) **No source patch is needed:** `mozjs` exposes a published **`intl` cargo feature** (`mozjs/intl` → `mozjs_sys/intl` → `dep:icu_capi`); turning it off makes `js/src/configure` run `--without-intl-api`. Disabling it also forces the from-source path (no matching prebuilt), which is exactly what the trim wanted. (2) **`autoconf2.13` is not required** (mozjs ships a pre-generated `js/src/configure`), and **no sudo was required**: `clang`/`clangxx`/`llvm-tools`/`lld`/`libclang` all come from **conda-forge** into an isolated env, and `cbindgen` from `cargo install`. The one non-obvious fix: bindgen's `libclang` does not inherit the clang driver's gcc-toolchain probe, so `CLANGFLAGS` must point at libstdc++ (`-I/usr/include/c++/13 …`) or it dies with `'utility' file not found`. *Implementation:* `engine/js` now has `spidermonkey-noicu` (same embedding surface, `intl` off) alongside `spidermonkey` (full, prebuilt, unchanged/default); both share an internal `_sm` code marker so the `#[cfg]`'d SpiderMonkey code path is identical and they differ **only** in `intl`. `shell` forwards both. Strictly a build-config flag on the published crate — **never** JIT/GC/sandbox (boundary respected). *Measured, with a control:* headless stripped release **38.0 MB → 17.6 MB, saving 20.4 MB (53.7%)**. Because noicu is from-source and the intl build is prebuilt, a **control build (from-source *with* intl)** isolates the variables: prebuilt−vs−source = **0.5 MB**, so ICU/`intl`'s **true** cost is **19.8 MB (46.9%)** — the old ~14.3 MB figure undercounted (it only measured `icu_data.o`, missing ICU code + ICU4X). *Verified, not assumed:* the ICU-free engine still runs real JS (JIT loop → `599994`, regexp → `99`, JSON → `3`) while `typeof Intl === "undefined"` → `1` and `new Intl.NumberFormat(...)` throws; `libjs_static.a` has **11,492** ICU symbols with intl and **0** without. *Tradeoff (why opt-in, not default):* `--without-intl-api` deletes the whole `Intl` web API — a real spec regression — so `spidermonkey` stays the default. The surgical variant (**keep `Intl`, ship en-only ICU data**) is now unblocked by the working from-source path and is the tracked follow-up. Platform: built + verified on Linux x86_64; [XP] feature, per-OS from-source builds unverified on macOS/Windows. Full numbers + repro in `bench/BASELINE.md` §1.
- **C3 dependency-graph hygiene.** *Finding (own `cargo tree`, 2026-07):* the earlier "two `tiny-skia`/`cssparser` versions" note was inaccurate. In the **default** build there is **one** `cssparser` (0.34); the second (0.37) appears **only** under `--features stylo` (Stylo brings its own). The duplicate `tiny-skia 0.11.4` is a **transitive** of `winit → sctk-adwaita` (Wayland client-side decorations, `gui`+Linux only) — not a competing copy of our paint's 0.12. The default **engine + agent** graph pulls **0** `wgpu`/`winit`/`sctk` (all correctly gated behind `shell`'s `gui` feature); remaining dupes (hashbrown/foldhash/getrandom/linux-raw-sys/phf) are deep transitive churn with low payoff. *Disposition:* **no action needed structurally** — gating is already correct. Optional low-priority trim: drop winit's `sctk-adwaita` default feature if we forgo Wayland CSD (removes the 0.11.4 `tiny-skia`), a measured UX tradeoff. Adopt `cargo tree --duplicates` + `cargo bloat` + `cargo-udeps` as **standing** measurement tooling (§8) rather than one-off audits. No crate to add, nothing to hand-roll. No WPT target.

### 4. Agentic capability (headless phase)
- **Observation:** viewport-clipped text; an accessibility/semantic tree; element
  bounding boxes so the agent can click by coordinate, not only by link index.
- **Actions:** typing, form submit, back/forward, wait-for-load, scroll-to-element,
  tab management.
- **Reliability/cost:** structured tool-calling where supported; retry/rate-limit
  handling; screenshot/response caching; token budgeting; deterministic replay.
- **Backend breadth:** the `InferenceBackend` trait is provider-agnostic — add local
  (llama.cpp/Ollama) and other cloud backends without touching `run_task`.

### 5. Correctness & conformance
- Stand up the **upstream WPT runner** (`$WPT_DIR`); grow the built-in reftests.
- Secondary real-site screenshot diffing vs a real Chrome render.
- Property/fuzz testing of parsers + layout — but **never** the JIT/GC fuzzing that
  would tempt crossing the SpiderMonkey boundary; that stays vendored.

### 6. Portability
- Keep Linux/macOS/Windows co-equal: verify builds + rendering on all three (only
  Linux is exercised so far); no platform-primary; `io_uring` stays out of the GUI.

### 7. Security & process model
- Move toward isolate/compartment-per-tab multiplexed over a small number of OS
  processes (not one-per-tab); site isolation for untrusted content; keep untrusted
  JS inside the vendored SpiderMonkey sandbox; TLS stays `rustls`.

**Research dispositions — Axis F: legitimate human-operator presentation (2026-07). Bounded to *not being wrongly walled as a bot when a human drives* — never scraping/agent evasion. Any mechanism indistinguishable from bot-evasion is out of scope by construction.**
- **F1 why non-Chromium engines get wrongly walled — the honest line.** *Finding:* CDNs (Cloudflare/Akamai) key on **TLS ClientHello (JA3/JA4** — cipher suites/extensions/curves; JA4 sorts extensions), **HTTP/2 fingerprint** (Akamai: SETTINGS/pseudo-header/priority order), **header order/casing**, **`User-Agent`**, and **JS-environment probes** (developers.cloudflare.com/bots JA3-JA4; blog.cloudflare.com/ja4-signals). The tooling that *matches* these (curl-impersonate-style "mimic Chrome at the cryptographic level: JA3/JA4, Akamai fp, header order") is exactly bot-evasion. Separately, `Mozilla/5.0` is a **universal legacy token every browser sends** (incl. Firefox `Mozilla/5.0 … Gecko/… Firefox/…`) — a general "modern browser" marker, *not* competitor impersonation; WHATWG itself acknowledges UA-discrimination wrongly blocks browsers (whatwg/html#10518; MDN UA reference; webaim UA history). *Disposition — the line drawn explicitly:* **IN SCOPE (honest presentation → `engine/net` + JS env):** (1) a **truthful** UA identifying Manuk + its real engine/version; the universal `Mozilla/5.0 (…) … Manuk/<ver>` general-token convention is a *defensible honest* form (all browsers use it, no competitor product token) but **flag it for human sign-off** as a judgment call; (2) a complete, correct, **consistently-ordered default header set** (Accept / Accept-Language / Accept-Encoding once B3 lands); (3) a **correct modern TLS handshake** via rustls (TLS1.2/1.3, current suites, SNI, ALPN) — our *honest* fingerprint; (4) a **spec-complete JS/DOM environment** (D3) so capability probes pass because we *actually implement* the APIs. These are "be a correct browser," verifiable and honest. **OUT OF SCOPE (impersonation ≡ scraping evasion, excluded per non-goals):** JA3/JA4 ClientHello mimicry; Akamai h2 fingerprint mimicry; UA spoofing to claim Chrome/Safari (`AppleWebKit/537.36 … Chrome/…`); copying Chrome's exact header order to match its fingerprint; faking Chrome-specific JS quirks / anti-fingerprint randomization. **Never reshape our fingerprint to match a specific mainstream browser.** Requests that *only* Chrome-fingerprint impersonation would satisfy are out of scope → **F2**. *Roadmap:* add an "honest presentation" clause to §7 — truthful UA (Mozilla-token pending sign-off), correct/ordered headers, honest rustls TLS, spec-complete JS env (D3); explicitly no fingerprint mimicry. *No WPT target* (presentation/policy, not conformance).
- **F2 graceful-degradation path (UX honesty, not evasion).** *Finding:* Cloudflare **documents** the `cf-mitigated: challenge` response header — present on **all** challenge-page types, `content-type: text/html` — specifically so legitimate clients can *detect* a challenge response (developers.cloudflare.com/cloudflare-challenges/…/detect-response). This is a **first-party, honest signal** (no reverse-engineering); other CDNs surface analogous block/challenge responses (403/429 + interstitial). The evasion path is to *solve* the challenge (Turnstile-mimicry, curl-impersonate) — out of scope. *Disposition:* **hand-roll a respectful shell interstitial (in scope, UX).** When a navigation resolves to a hard-wall — detected honestly via `cf-mitigated: challenge` (or repeated 403/429 + a challenge interstitial) — the shell shows a clear, calm page: "This site blocks browsers other than the mainstream ones; Manuk won't impersonate another browser to get in," with honest options (retry, copy URL, open in another browser). **Never attempt to solve or bypass the challenge** (that is evasion). This reframes CLAUDE's "excluded hostile tier" from an engineering arms race into an honest UX state; detection reuses the documented header, so the mechanism is the *opposite* of bot-evasion (we surrender honestly rather than evade). *Roadmap:* add a "graceful-degradation interstitial" item to §7 / shell UX — honest challenge detection → respectful "site blocks this browser" page, explicitly no challenge-solving; connects to the §Non-goals excluded tier. *No WPT target.*

### 8. Measurement (cross-cutting)
The targets are only real if measured. Stand up benchmarks/CI for binary size,
per-tab baseline memory, click-to-navigate latency, and frame timing — so every
change can report before/after numbers as the working agreements require.

### 9. User-facing features (headful) — Axis E

**Research dispositions — Axis E pass-1 baseline (2026-07, 2 searches/item). "Warrants deeper research" = pass-2 flag.**
- **E1 Chrome UI essentials.** *Finding:* find-in-page is a browser-UI **overlay** over the renderer's text (Chromium keeps it an overlay to avoid relayout); full-page **zoom** changes the CSS-px↔device-px ratio → **layout reflow** (crisp text), whereas pinch-zoom is a post-render **surface transform** (no layout) (chromium.org find-in-page/find-bar; MDN zoom/scale, viewport concepts). *Disposition:* **confident — hand-roll shell UX, every primitive already in the engine.** Tabs/windows/history/bookmarks/settings/URL-suggestions = standard shell state (winit + a small local store + per-tab nav stack); **find-in-page reuses the fragment tree** (`TextFragment` runs + `visible_text` → match text, map to rects, overlay-highlight); **full-page zoom = relayout at a zoom factor** (reuse `layout_document`, crisp), **pinch-zoom = compositor surface transform**. No new engine surface. *Roadmap:* §9 shell-UX item; no WPT target.
- **E2 local encrypted password store + autofill — PASS-2 RESOLVED (security-critical; audited-crate orchestration only, zero hand-rolled crypto).** *Finding:* Chromium's model = a random key in the **OS secret store** encrypts the DB at rest — Windows **DPAPI**, macOS **Keychain** (16-byte key → PBKDF2), Linux **Secret Service/libsecret** (with a weak `v10` hardcoded-password *fallback* when no keyring). Autofill matches by **origin (scheme+domain required)**; same-site sharing (`www`↔`m.example.com`) is scoped by the **Public Suffix List (eTLD+1)**; cross-origin-iframe credential leakage is a real past vuln class; Apple maintains shared `password-manager-resources` (domain-equivalence quirks) (chromium.org os_crypt / os-x-keychain; developer.chrome.com identity; github.com/apple/password-manager-resources). *Disposition:* **reuse audited crates ONLY, never hand-rolled crypto** — `keyring` (wraps DPAPI/Keychain/Secret Service), a `RustCrypto`/`ring` AEAD (AES-256-GCM or ChaCha20-Poly1305) for at-rest with the OS-store key, `psl`/`publicsuffix` for eTLD+1 scoping, reuse Apple's resource list for quirks; hand-roll only orchestration + origin-matching policy. **Flagged pass-2** for the *policy* architecture (NOT crypto primitives): the Linux-no-keyring fallback stance (Chromium's weak default is a known problem — refuse? require an app primary password?), the exact origin-match rules (cross-origin iframe, HTTP↔HTTPS downgrade, PSL scoping — a wrong rule leaks credentials), and whether to require a primary password. *Pass-2 resolution (2026-07):* **Key management** — the primary encryption key lives in the **OS secret store via `keyring`** (v4; DBus Secret Service / macOS Keychain / Windows). When it is **unavailable** (headless / no Secret Service — Chromium's weak spot), fall back to a **primary-password-derived key via `argon2` (Argon2id, PHC winner)** — **never** Chromium's hardcoded-password fallback; the DB is sealed with a **RustCrypto AEAD (`chacha20poly1305` or `aes-gcm` — both NCC Group-audited, no significant findings)**; the KDBX4/KeePass format (Argon2id + AEAD) is a candidate audited on-disk format (github.com/open-source-cooperative/keyring-rs; RustCrypto/AEADs NCC audit; P-H-C/argon2). **Origin matching** — Chromium keys credentials by **`signon_realm` = scheme+host+port** (exact match = default fill); **PSL/eTLD+1 matching applies only to HTML forms** and is surfaced as a *related-domain* suggestion (not silent), via the `psl` crate; **no scheme-downgrade** (an HTTPS-saved credential is never auto-filled into HTTP); **cross-origin iframes** require the field's own origin to match (the documented leakage class — no silent cross-origin fill); affiliation (Digital Asset Links / Related Website Sets) is opt-in + verified via a well-known file → **defer** (chromium password_manager signon_realm/PSLMatchingHelper; developer.chrome.com seamless-credential-sharing). *Disposition:* **reuse audited crates only** — `keyring` + RustCrypto `chacha20poly1305`/`aes-gcm` (audited) + `argon2` + `psl`; **hand-roll only orchestration + the origin-match policy**; the constraint (no hand-rolled crypto primitives) is satisfied. *Roadmap:* §9 + cross-ref §7 Security; no WPT target. **Landed (2026-07-10, `manuk-store` crate):** audited-crate orchestration, **zero hand-rolled crypto** — at-rest sealing via **`chacha20poly1305`** (RustCrypto AEAD, `nonce||ciphertext`), the no-keyring key via **`argon2`** Argon2id, related-domain scoping via **`psl`**, and the OS secret store via **`keyring`** (DPAPI/Keychain/Secret Service) behind the `os-keyring` feature (builds; needs a running keyring service, so the Argon2id path is the verified one — no hardcoded fallback). **Origin policy** (`autofill(field_url)`, called with the FIELD's own origin): exact `signon_realm`=scheme+host+port → auto-fillable; same eTLD+1 + same scheme → a *suggestion* not silent; **no scheme-downgrade** (https cred never offered to http); a cross-origin iframe field only sees its own origin's creds. Verified by 7 tests incl. the security-critical cases: AEAD wrong-key/tamper rejection, no cross-origin leak, no scheme downgrade, cross-iframe uses the field origin. **Remaining:** per-OS keyring CI (each backend) + the D3 form-field/origin plumbing that calls `autofill` on real pages; affiliation (Related Website Sets) deferred. *Roadmap:* §9 + cross-ref §7 Security; no WPT target.
- **E3 translate-page.** *Finding:* Firefox Translations = fully **local** on-device NMT (Bergamot/Marian → WASM, CPU-optimized), models **~20 MiB per language pair downloaded on demand** (not bundled → base binary stays lean) + inference runtime; entirely private (mozilla blog/hacks Bergamot; firefox-source-docs translations; mozilla/firefox-translations-models). In-page translation **block-segments** the DOM, flattens inline runs (`<a>/<b>/<i>`) with boundary markers, translates, and reinjects preserving structure. *Disposition:* **confident — reuse, two lean tiers, hand-roll the DOM glue.** (1) **BYO-endpoint (lead):** reuse **E6's `InferenceBackend`** to translate — zero model runtime, user's configured endpoint (privacy = user's choice). (2) **Local private tier:** reuse Bergamot/Marian models on-demand via FFI to `bergamot-translator` or ONNX (`ort`) — the local-runtime pick is a deferred sub-decision behind E6. DOM extraction/reinjection reuses our block/inline distinction (layout already separates them). *Roadmap:* §9 item, reuses E6; no WPT target.
- **E4 developer tools / console — PASS-2 RESOLVED (strategic: implement a WebDriver BiDi remote end, not CDP).** *Finding:* CDP is Chromium-only and **not a standard**; **WebDriver BiDi** is the **W3C** cross-browser automation standard (bidirectional/evented like CDP but interoperable) — Firefox ships it production-ready, Puppeteer & Playwright support it, and it is **WPT-tested** (`webdriver/tests/bidi/`, wdspec) with modules session/browser/browsingContext/script/network/log/input/storage (developer.chrome.com/blog webdriver-bidi; w3.org/TR/webdriver-bidi; web-platform-tests/wpt). *Disposition lean (validate in pass-2):* implement a **WebDriver BiDi subset**, **not CDP** — this makes the *agent* drivable by Puppeteer/Playwright (the two-birds) **and** is standards-aligned + WPT-verifiable (fits WPT ground-truth, avoids CDP's Chromium-quirk surface, itself a non-goal); a bespoke inspector only for the human DevTools *UI*. **Flagged pass-2:** the minimal viable module subset (session + browsingContext + script + network + log?), the implementation effort, and how BiDi's session/navigable model maps onto the existing `AgentBrowser`/`run_task` + `engine/net`. *Pass-2 resolution (2026-07):* BiDi is **JSON-RPC over WebSocket** — a *remote end* (server) = a WebSocket endpoint (**reuse `tokio-tungstenite`, already in our stack**) + a JSON command dispatcher matched against the spec's CDDL, with `session.subscribe` for events; `chromium-bidi` and Firefox's `remote/` are references (w3.org/TR/webdriver-bidi; github.com/w3c/webdriver-bidi; GoogleChromeLabs/chromium-bidi). The commands mostly **wrap operations we already have**: `browsingContext.navigate`→`AgentBrowser.navigate`, `browsingContext.captureScreenshot`→`screenshot_png`, `getTree/create/close`→tab model, `network.*` events→`engine/net`, `log.entryAdded`→console. **Minimal buildable-NOW subset (pre-D3):** `session` + `browsingContext` (navigate/captureScreenshot/getTree/create/close + load events) + `network` events + `log` — this alone makes the agent **Puppeteer 23+-drivable today** (the two-birds), reusing `AgentBrowser` + `engine/net`. **Deferred sub-commands:** `script.evaluate`/`callFunction` (blocked on D3 JS/DOM bindings), `input.performActions` (blocked on hit-testing + element refs — ties to G-c a11y tree). Moderate effort (WebSocket + JSON dispatcher + CDDL-shaped types); Puppeteer is more BiDi-ready than Playwright (still partial, microsoft/playwright#32577). *Disposition:* **build the BiDi remote-end subset above** on `tokio-tungstenite`. *New WPT target:* `webdriver/tests/bidi/`. *Roadmap:* §9 (+ §4 agentic two-birds). **E4 LANDED (2026-07-10, new crate `bidi` = `manuk-bidi`, default build) — and the headline acceptance is VERIFIED, not assumed.** Layering: `protocol` holds the message shapes + command dispatcher and is **transport-free** (so the whole protocol is unit-testable without a socket); `server` is a thin `tokio-tungstenite` shell. **Commands:** `session.{status,new,end,subscribe,unsubscribe}`, `browser.{getUserContexts,getClientWindows,close}`, `browsingContext.{getTree,create,close,navigate,captureScreenshot,setViewport,traverseHistory}`, and `input.performActions` (pointer click → reuses §4a hit-testing + §4b `click_at`). Events: `browsingContext.{contextCreated,contextDestroyed,navigationStarted,domContentLoaded,load}` and a synthetic `network.responseCompleted` for the document load; a subscription to a **module** (`"browsingContext"`) covers its events, as the spec requires. Each context is its own `AgentBrowser`, so BiDi and the agent share one page pipeline. **Concurrency note:** the pipeline is `!Send` (`FontContext` holds `Rc<fontdue::Font>`), so each connection gets its **own thread + current-thread runtime** (the accepted socket is handed over as a `std` socket and re-registered on the session's reactor) — real cross-client concurrency with each session pinned to one thread. **ACCEPTANCE MET (`bidi/tests/puppeteer/`): puppeteer-core 23.11.1 on Node v22.18.0 connects, `newPage()`, `goto()`, and `page.screenshot()` (3497-byte real PNG) all succeed against the remote end.** `page.title()` fails — deliberately: it is implemented via `script.callFunction`, which we return `unsupported operation` for rather than faking, since it needs the JS engine wired in (the documented residual). **Driving the real client found five protocol bugs a spec read had not** (all fixed, all recorded in the README): (1) `browser.getUserContexts` is required before any context work; (2) `browsingContext.contextCreated` must be emitted **before** the `browsingContext.create` reply — clients build their context map from the *event* and look the id up the instant the command resolves, so emitting after loses the race and the client reports *"failing to create a browsing context correctly"* (this is why `Dispatched` has `before`/`after`); (3) context info must be **complete** (`parent`, `userContext`) or clients silently drop the event, which is indistinguishable from "never created"; (4) `newPage()` issues `browsingContext.setViewport` (→ new `AgentBrowser::set_viewport`, relayout + scroll clamp); (5) `goto` needs the full navigation lifecycle — `navigationStarted` **before** the reply, then `domContentLoaded` + `load` **carrying the same `navigation` id** and a numeric `timestamp`, or the client ignores them and `goto` never resolves. **Verification:** 11 protocol tests + 3 real-WebSocket integration tests (a client drives a full session; a malformed frame errors with `id:0` **without dropping the connection**; two clients get independent sessions) + 1 doc test; workspace green (38 ok blocks); clippy-clean. Runnable binary: `cargo run -p manuk-bidi --bin manuk-bidi -- --port 9222`. **Documented gaps (not faked):** `script.*` (needs D3/SpiderMonkey); the full `input` grammar (keys, wheel, multi-pointer, durations); per-request `network.*` events (only a synthetic document response); no `browsingContext` frame/iframe tree. Platform: [XP] pure-Rust; verified on Linux.
- **E5 extensions.** *Finding:* the full WebExtensions surface is enormous (hundreds of APIs, content scripts, background workers, permissions) — a scope trap and a major attack surface. But the #1 high-value case (ad/tracker/content **blocking**) has a **declarative, code-free** path: Manifest V3's `declarativeNetRequest` replaced runtime `webRequest` interception with declarative URL-filter rules (safer, privacy-preserving, no host permissions), and Brave's **`adblock`** (adblock-rust) is a mature Rust crate — EasyList/EasyPrivacy/uBO-syntax network blocking + cosmetic filtering, tokenized matching (69× speedup), **designed to run inside a native browser core** (developer.chrome.com declarativeNetRequest; github.com/brave/adblock-rust; brave.com blog). *Disposition:* **confident, skepticism upheld — extensions-as-a-runtime is OUT OF SCOPE** (scope trap + attack surface; content scripts = arbitrary JS injection, excluded); password managers are native (E2), not extensions. The valuable subset is delivered **natively without any extension runtime**: **reuse `adblock`** in `engine/net` (+ cosmetic hooks into DOM/paint) consuming standard filter lists — this is a §2/§7 content-blocking feature, not "extensions." *Roadmap:* §9 rejects an extension runtime; add native content-blocking (`adblock`) to §2 loading / §7 privacy; no WPT target.
- **E6 bring-your-own-key in-browser AI — PASS-2 RESOLVED (security model: layered CaMeL structural separation + OWASP Action-Guard).** *Finding:* the panel is architecturally trivial — **reuse the `InferenceBackend` abstraction**; the headful AI panel and the headless agent are *the same core* with different front-ends (the project thesis). The crux is security: **indirect prompt injection** is a systemic, unsolved challenge for AI browsers — page content (incl. hidden white-on-white text, HTML comments, and **screenshot-borne "unseeable" injections**) gets fed to the LLM without separating user instructions from untrusted content, causing cross-site actions (fetch OTPs, banking) (brave.com agentic-browser series; simonwillison.net; arXiv:2511.19477). Anthropic's Claude-in-Chrome documents the mitigation stack: **per-site permissions** (first line of defense), **explicit confirmation for sensitive actions** (publish/purchase/share — retained even in autonomous mode), **system-prompt hardening**, and **classifier detection** of injections in untrusted content — reducing a challenge-set attack rate 35.7%→**0%** (anthropic.com/news/claude-for-chrome, /research/prompt-injection-defenses). *Disposition (pass-1):* **reuse `InferenceBackend` (confident); the security/consent model was the open gap → resolved in pass-2 below.** **Critically, the screenshot-injection vector applies to our EXISTING agent path** (which already sends screenshots) — so this is also a §4/§7 retrofit, not only a new panel. **Flagged pass-2:** per-site permission model, sensitive/cross-origin action taxonomy + confirmation UX, structural instruction-vs-page-content channel separation in `run_task`, and whether to add an injection classifier pass — for **both** the panel and the agent. *Pass-2 resolution (2026-07) — a layered architecture, all reuse of established patterns (no novel crypto/ML):* **(1) Structural (strongest) — dual-LLM / CaMeL:** the privileged planner sees only the **trusted user task** and emits a plan; **untrusted page content (incl. screenshot-derived text) is data with provenance**, processed by a quarantined path with **no tool access**; a taint/capability check gates every tool call — "structurally unable to influence flow, like parameterized queries for SQLi" (arXiv:2503.18813 CaMeL; simonwillison.net/2025/Apr/11/camel). **(2) Per-site permissions** (Anthropic's first line of defense). **(3) OWASP Action-Guard human-in-the-loop:** classify actions by **irreversibility heuristics** (financial / administrative / externally-visible / data-deletion / privilege-escalation / cross-origin credential use) and require explicit confirmation, **separating decision from execution** (genai.owasp.org LLM06/LLM08; AI Agent Security Cheat Sheet). **(4) Injection classifier** over untrusted content (Anthropic 35.7%→0%). **Concrete Manuk change:** retrofit `run_task` so the `Observation` (page text + screenshot) is untrusted, provenance-tagged data **kept out of the instruction/planner channel** (we currently merge it — the Comet flaw), and gate cross-origin navigate/click/form-submit behind capability + confirmation. **This hardens BOTH the future panel AND the existing agent path** (which already sends screenshots — the unseeable-injection vector). *Disposition:* **reuse `InferenceBackend` (arch) + the layered CaMeL/permissions/Action-Guard/classifier defense (security)**; the classifier runs via the existing backend. *Roadmap:* §9 (+ §4 agent, §7 security). *No WPT target.*
- **E7 profiles / containers / VPN.** *Finding:* Firefox distinguishes **profiles** (complete separation of *all* data — bookmarks/passwords/settings/extensions/history/cookies) from **containers** (Multi-Account: separate only cookies/logins/site-data *within* a profile — a contextual-identity cookie-jar partition), with **Total Cookie Protection** = a per-website cookie jar (partitioned storage) (support.mozilla.org containers/profiles/TCP). SOCKS5/HTTP proxy is application-level, unencrypted routing; `tokio-socks` is the mature async Rust client; `wireproxy` even exposes WireGuard *as* a SOCKS5 proxy — so "VPN" can reduce to "point at a proxy" (github.com/sticnarf/tokio-socks; github.com/windtf/wireproxy). *Disposition:* **confident, lean framing.** Profiles/containers = a **storage-partition** design (profile ⊃ container jar ⊃ per-site jar/TCP) — **hand-roll the partition key** over the storage layer, but **defer, blocked on a storage layer** (we have none: no cookies/cache/history/localStorage yet). **VPN = reuse SOCKS5/HTTP proxy** (`tokio-socks`) so the browser routes through a user-provided proxy (WireGuard via `wireproxy` externally); **bundling a WireGuard/OpenVPN client is out of scope** (scope-heavy, no strong reason) — the RESEARCH-recommended lean framing. *Roadmap:* §9 (profiles deferred on storage; SOCKS proxy in §2 net); no WPT target. **E7 LANDED (2026-07-10, `engine/net::{cookies,storage,proxy}`, default build).** The item's stated prerequisite — "begins by building the storage layer" — is honored: **(1) `cookies` = an RFC 6265 jar.** Parses `Set-Cookie` (Domain/Path/Secure/HttpOnly/SameSite/Max-Age/Expires incl. an IMF-fixdate parser verified against the RFC's canonical `784111777`), and enforces the rules that carry security weight: **`Domain` may not be a public suffix** (`Domain=com` / `Domain=co.uk` rejected via `psl` — the classic supercookie hole; unknown suffixes **fail closed**), `Domain` must domain-match the request host, **host-only** cookies never reach subdomains, `Secure` cookies never go over `http`, **an insecure origin may not *set* a `Secure` cookie** ("Leave Secure Cookies Alone" — else plaintext http overwrites the https session), `Max-Age` beats `Expires` (§5.2.2), path-matching respects segment boundaries (`/application` ∌ `Path=/app`), and send-order is longest-path-then-oldest (§5.4.2). **(2) `storage` = the profile ⊃ container ⊃ top-level-site partition.** Two containers keep **separate jars for the same site** (the item's headline acceptance); profiles are fully isolated; and keying on the *top-level site* gives **unconditional Total Cookie Protection** — the same tracker embedded on `news-site.org` and `shop-site.com` gets two different jars and cannot join the visits. `localStorage` is partitioned by (partition, script origin); history is per-**profile** (survives a container reset, the Firefox model); `clear_container`/`clear_profile` implemented. **`SameSite` is enforced here**, not in the jar, because it needs request context — and the *asymmetry is implemented correctly*, which is the easy thing to get wrong: a **subresource** is cross-site vs. `top_level`, but a **top-level navigation** must be judged against the **initiator**, since the destination *becomes* the top level. Comparing a navigation against `top_level` would ship `SameSite=Strict` cookies on a cross-site link click — precisely the CSRF case `Strict` exists to prevent. Tests cover cross-site subresource (only `None` rides), cross-site link click (`Lax`+`None` ride, `Strict` never), address-bar navigation (no initiator ⇒ `Strict` rides), and same-site navigation. **(3) `proxy` = SOCKS5 via `tokio-socks`** (the sanctioned reuse). The target host is handed to the proxy **as a domain name, never resolved locally**, so DNS happens at the proxy and does not leak the destination; TLS through the proxy keeps the normal webpki verification path (proxying must never weaken TLS). `fetch_via_proxy` drives one request over hyper's low-level `client::conn` (a proxied connection is per-request config, not a process-global pool). **Verified behaviorally, not asserted:** the test spins an **in-process SOCKS5 server** (no-auth CONNECT, IPv4+domain ATYP) in front of an in-process origin, fetches through it, and asserts the proxy saw **exactly one** connection; a second test proves a **dead proxy errors instead of silently falling back to a direct connection** (a silent fallback would leak the user's IP). **Bundling a WireGuard/OpenVPN client remains out of scope** (use `wireproxy` → local SOCKS port). **Documented gaps (not faked):** storage is **in-memory** — no disk persistence or at-rest encryption yet (that reuses E2's AEAD; tracked follow-up); no HTTP cache; no cookie count/size quotas, no `__Host-`/`__Secure-` prefix enforcement, no CHIPS `Partitioned` attribute (our partitioning is unconditional); no proxy connection pooling, no SOCKS4/HTTP-CONNECT proxies. **Not yet wired into the fetch path** — `fetch`/`fetch_streaming` do not consult the jar or proxy yet; that is the integration follow-up. 33/33 `manuk-net` tests green (34 with `adblock`), clippy-clean, workspace green. Platform: [XP] pure-Rust; verified on Linux.

### 10. Blue-ocean capabilities — Axis G

**Research dispositions — Axis G pass-1 baseline (2026-07, 2 searches/item). Assessed: real utility vs demo, engine surface needed, uniquely enabled by our shared-core architecture.**
- **G-a unified human+agent session.** *Finding:* NO LONGER novel — human↔agent live-session handoff is a crowded 2025–26 space: Tandem Browser ("AI in the browser itself, same tabs/cookies/session, explicit handoffs, tab locks"), Gemini-in-Chrome auto-browse, chrome-use, browser-act (cross-device handoff) all ship take-over-anytime in the user's live session (github.com/hydro13/tandem-browser; blog.google gemini-chrome). *Disposition:* **confident — real utility, architecturally natural on our shared `engine/page` core (reuse engine/page + the agent core), but NOT blue-ocean** (market caught up). Pursue as a low-marginal-cost feature of the shared-core design, not a moonshot; the *architectural* cleanliness (both front-ends on one core, deterministic handoff) is the only differentiator vs CDP/Electron bolt-ons. *Roadmap:* §10 natural feature; no WPT target.
- **G-b local-first semantic history — PASS-2 RESOLVED (stack is off-the-shelf; the storage tension is solved by LEANN-style recompute).** *Finding:* on-device semantic search is active research (LEANN low-storage index; local embedding models; ~7× embedding storage blowup is the key constraint) but a **browser-integrated private semantic index of *actually-read* page content** — uniquely enabled by our engine already parsing every page to clean text + the local BYO-AI (E6) — is **not a mainstream feature** (browser history is keyword-only) (arXiv:2506.08276 LEANN; github vector-search). Volatility scoring is grounded: per-URL adaptive re-embed interval by observed change rate (code-docs ~0.01%/day → weeks; news ~80%/hour → minutes) + content-addressing for dedup/change-detection. *Disposition:* **warrants deeper research** — genuinely differentiated and rides on surface we build anyway (clean text + local AI). **Flagged pass-2:** the vector-store choice (LEANN-style / `usearch` / sqlite-vec), the local embedding model, the storage budget vs §3 lightweightness (7× blowup), the volatility-scoring policy, and index privacy/encryption. *Pass-2 resolution (2026-07) — the whole stack is off-the-shelf, and the storage tension is solved:* **Embeddings** — reuse **`fastembed`** (Rust, ONNX, no-Tokio) with a **small quantized** model (BGE-small-Q / MiniLM), downloaded once then offline/on-device/private (github.com/anush008/fastembed-rs). **Vector index** — reuse an embedded store: **`sqlite-vec`** (usearch-backed HNSW, single-file, no server — the lean default) or LanceDB (github.com/asg017/sqlite-vec; lancedb). **The §3 storage tension is resolved by `LEANN`** (MLsys2026, arXiv:2506.08276): store only a **pruned proximity graph** and **recompute embeddings at query time** with the same encoder → **~97% storage savings / >50×**, <5% overhead vs raw, 90% top-3 recall, <2s search — and since we *already* have the encoder (`fastembed`) and re-parse every page, recomputation is natural. **Volatility** — per-URL adaptive re-embed interval by observed change rate + content-addressing for dedup/change-detection (pass-1). **Privacy** — the index is 100% local; **encrypt at rest reusing the E2 AEAD stack** (`chacha20poly1305` + OS-store/primary-password key). *Disposition:* **reuse — off-the-shelf mature crates + one research technique (LEANN) that resolves the lightweightness conflict.** Uniquely enabled by our parsed-clean-text + local BYO-AI (E6) + local-first ethos; the "stronger than expected" read holds and is now de-risked. *Roadmap:* §10 (+ ties E6, §3, and the E2 AEAD). No WPT target.
- **G-c agent-assisted accessibility.** *Finding:* the "one investment, two payoffs" thesis validates strongly. **`accesskit`** is a mature Rust cross-platform a11y bridge (Windows UIA / macOS NSAccessibility / Linux AT-SPI; tree schema modeled on Chromium's a11y abstraction; used by egui). Independently, web-agent research (WebArena et al.) uses the **accessibility tree** as the *preferred* observation channel — "a subset of the DOM with role/text/properties, structured yet more compact than DOM," read via semantic roles/labels "rather than raw pixels or full DOM" (github.com/AccessKit/accesskit; arXiv:2307.13854 WebArena; arXiv:2508.04412). *Disposition:* **confident — high-leverage, build ONE a11y/semantic tree** (DOM + ARIA/implicit roles + layout geometry): **reuse `accesskit`** for the screen-reader platform bridge; **hand-roll** the DOM→tree mapping (WAI-ARIA + HTML-AAM). Two payoffs: (1) human screen-reader accessibility; (2) the agent's **structured observation channel** (§4/D3 — better than raw text+screenshot, and reduces reliance on the injection-prone screenshot channel, cross-ref E6). This is engine surface D3 needs anyway → sequence together. *Roadmap:* §10 (+ D3, §4, E6). *New WPT target (partial):* `wai-aria/` for role computation. **§4a LANDED (2026-07-10, new crate `engine/a11y` = `manuk-a11y`, default build, no feature gate):** the engine-side half — a hand-rolled DOM→semantic tree. `Role` (28-variant ARIA subset) + `role_of()` implementing **HTML-AAM implicit roles** (incl. the real rules: `<a>` is a `link` only *with* `href`; `<section>` is a `region` only *when named*; `<th scope=row>`→`rowheader`; `<input type=…>`→checkbox/radio/button/textbox) with **explicit `role="…"` override** honoring ARIA's *first-valid-token-wins* (invalid tokens fall through to implicit). `is_hidden()` prunes `aria-hidden=true`, `hidden`, `<input type=hidden>`, and non-rendered tags (`script`/`style`/`head`/…). `<img alt="">` is **presentational**: the node is dropped but its children are **reparented** (matching `role=presentation`). `accessible_name()` implements a pragmatic **accname** precedence: `aria-labelledby` (one-level deref) → `aria-label` → native (`<img alt>`, `<label for>`, `value` for button-inputs, `placeholder`) → *name-from-content* (only for roles that allow it) → `title`; all whitespace-normalized. `build_tree()` yields `A11yNode{node, role, name, bbox, children}` and `to_observation_lines()` renders `role "name"` lines, dropping unnamed `generic` containers as noise. **Payoff 2 wired:** `manuk-agent`'s `Observation` gained `semantics: Vec<String>` (populated via the new `Page::dom()`), rendered into the prompt as an `ACCESSIBILITY TREE` block — **inside the E6 untrusted fence**, because role/name text is page-derived and an `aria-label="ignore prior instructions"` is an injection vector exactly like link text (test `accessibility_tree_is_inside_the_untrusted_fence` asserts both the section and an adversarial name sit strictly between the fence markers). **Verified:** 8/8 `manuk-a11y` tests, incl. one over **real html5ever-parsed HTML** (exercises implied `<html>`/`<head>`/`<body>`, `<head>`-subtree pruning, single `document` root, label-beats-placeholder, decorative-image drop); workspace green (34 ok blocks, `--all-targets`), clippy-clean. **§4a GEOMETRY LANDED (2026-07-10) — and it exposed a real engine gap.** `bbox` is now populated from the layout fragment tree via `build_tree_with_rects` + the new `Page::a11y_tree()` + `manuk_layout::LayoutBox::node_rects(&dom)`. **The gap:** inline elements produce **no `LayoutBox`** — their text becomes `TextFragment`s inside the containing block's inline formatting context, which dropped node identity. So `<a>` and `<button>` — *exactly* the elements an agent clicks — had **no geometry at all** (caught by a real-pipeline test, not by assuming). *Fix:* `TextFragment` gained `node: Option<NodeId>` (deepest element ancestor of the run's text) + `width` (the advance, so a run's rect needs no re-measure), threaded through `InlineItem`→`LineFrag`→`TextFragment` in the inline collector/line-breaker; `node_rects` unions **block boxes ∪ inline runs**, and propagates each run into its **element ancestors** so `<a><em>x</em></a>` gives `<a>` a rect too. An inline element split across lines gets the union of its runs. New a11y API: `Rect::{center,intersects}`, `A11yNode::{to_viewport_lines,find,hit_test}` — `to_viewport_lines(viewport)` clips to what is on screen and appends each element's **click point** (`button "Sign in" @(60,40)`), omitting geometry-less nodes because an agent cannot click what has nowhere to click; `hit_test` returns the **smallest containing box** (deepest element); `find(role,name)` is the case-insensitive role-scoped lookup so an agent can say "click the *Sign in* button" without a CSS selector. **Wired into `manuk-agent`:** `Observation.semantics` is now the viewport-clipped, click-point-annotated rendering at the current `scroll_y` (falling back to the unclipped tree if the page produced no geometry, rather than silently reporting an empty page) — still emitted **inside the E6 untrusted fence**. **Verified end-to-end on the real pipeline** (`manuk-page::a11y_tree_carries_real_layout_geometry_and_hit_tests`: the button's bbox is non-degenerate, the `<h1>` lays out above it, hit-testing the button's own center returns that button, and the viewport line carries its click point) plus 3 layout tests pinning inline-run recovery, ancestor propagation, and the multi-line union. 24 layout / 11 a11y / 10 page / 11 agent tests green; workspace green (34 ok blocks); clippy-clean. **Documented gaps (not faked):** no `aria-owns` reparenting, no live regions, accname does one level of `aria-labelledby` deref rather than full recursion; **occlusion is not modelled** — `hit_test` picks the smallest containing box, which is not the topmost painted box under a `z-index` stack. The **`accesskit` platform bridge is still not wired** — this crate is the engine-side tree it will consume. Platform: [XP] pure logic, verified on Linux.
- **§4b agent actions + reliability — LANDED (2026-07-10, `manuk-agent`).** The agent could previously only navigate/click-a-link-index/scroll/finish. It now drives real pages. **Actions added** (all deserialized from the model's JSON): `type` (into a text field by accessible name), `click_text` (role + name, substring + case-insensitive), `click_at` (absolute document coordinate, hit-testing the §4a tree), `submit`, `back`, `forward`, `scroll_to`. **`AgentBrowser` gained** a real per-tab **history stack** (`back`/`forward`; navigating after a `back` **truncates** the forward entries, as browsers do; running off either end **errors** rather than silently no-op'ing, so the agent gets a fact); `type_into` (mutates the arena DOM's `value` + relayouts, so a later submit serializes what was typed); `activate(node)` returning an honest **`Activation`** (`Navigated(url)` / `Submitted(url)` / `Toggled(bool)` / `Inert`) that follows links, submits forms, and toggles checkboxes — a `<button type="button">` correctly does **not** submit; `click_by_name`, `click_at`, `scroll_to`. **New `agent::forms` module** models HTML form submission: `owning_form` (nearest `<form>` ancestor, else the document's first), `fields` (successful controls in document order — unchecked boxes omitted, `name`-less and `submit`/`reset`/`image`/`file` controls skipped, `<select>` honors `selected` else the first option, `<textarea>` prefers a typed `value` over its text children), and `submission_url` (resolves `action` against the page URL and percent-encodes the query). **`method=post` is REFUSED, not silently downgraded to GET** — a downgrade would push passwords/tokens into the URL and the `Referer`. **Action-Guard (E6) extended on a stated principle:** an action that can transmit page-derived or typed data to a **page-chosen destination** is the exfiltration channel a hidden injection reaches for, so `submit` and `click_text` on a *button* (which can submit) are **Sensitive**; `click_at` is Sensitive because a raw coordinate **cannot be checked against any target before it is clicked**; `type` is **Safe** because it is purely local (nothing leaves the machine until a submit/navigation); `back`/`forward`/`scroll_to`/`scroll` are Safe; a `click_text` on a *link* is judged by its href exactly like `click`. **Reliability:** `complete_with_retry` retries **transient** inference failures (429/5xx/timeouts/reset) with exponential backoff and **never retries permanent** ones (401/403/invalid key/unknown model) — retrying those burns budget and hides the real error; *permanent wins over transient* when an error string contains both. **Token budgeting:** `AgentConfig.history_turns` (default 6) bounds the retained (observation, action) exchanges so a long run cannot grow the prompt without bound; `max_retries`/`retry_base_delay_ms` are configurable. **Engine changes needed and made:** `Dom::remove_attr` (to *unset* boolean content attributes — `checked=""` is still checked); `a11y::Role::{parse,matches}` (role-token parsing + level-insensitive heading comparison) and `A11yNode::find_containing`. **Bug found by test:** `hit_test` broke area ties toward the *ancestor*, so clicking a lone `<button>` inside a `<form>` of identical size "hit" the form; ties now resolve to the **deeper** element (pre-order ⇒ `<=`), pinned by a regression test. **Verified hermetically** — every browser test drives the *real* navigate path through `data:` URLs (no network, no disk): typing lands in the DOM and produces `?q=rust+lang`; a checkbox toggles both ways (attribute really removed); a heading and a `type=button` are `Inert`; back/forward/truncation/end-of-stack-errors; a link click navigates to a `data:` destination and `back` returns; `click_at` on a button's center resolves to the button and submits its form. 27 agent / 12 a11y / 10 page tests green; workspace green (34 ok blocks); clippy-clean (incl. an MSRV fix — `is_none_or` needs 1.82, MSRV is 1.80). **Documented gaps (not faked):** POST forms, `<select multiple>`, `<input type=file>`, `formaction`, and `enctype` are not modelled; no tab management yet; deterministic replay is G-d. Platform: [XP]; verified on Linux.
- **G-d deterministic replay / provenance.** *Finding:* the established technique for reproducible agents is an **append-only event log recording model + tool responses** (models aren't reproducible, so you record their outputs), with strict-mode event-by-event divergence checking as a *proof* of reproducibility, and "decision-complete provenance" enabling re-evaluation of historical runs under new policy without re-execution (arXiv:2605.21997 "The Log is the Agent"; arXiv:2505.17716 record&replay). Crucially, **GPU rendering and layout are documented sources of replay-nondeterminism** (WebKit "Web Replay"; GPU kernel autotuning) — so GPU/cloud-VM agents diverge on replay. *Disposition:* **confident — real utility (auditable/reproducible agent runs for compliance/research/debug/regression), uniquely well-positioned.** Our agent is **display-free with a deterministic CPU raster** (`tiny-skia`) → stable screenshot hashes make exact replay achievable where GPU agents can't; the clean `InferenceBackend` seam records model I/O. **Reuse** the existing `AgentOutcome` transcript → extend to a full event log; low marginal cost. *Roadmap:* §10 (+ §4 agent, §5 correctness — replay doubles as a regression harness). No WPT target.
- **G-e instant per-tab resource honesty.** *Finding:* real pain (the founding "why is my fan spinning") but **not novel** — Chrome/Firefox already ship per-tab/per-process task managers + `about:memory`. The genuine wrinkle is **ours specifically**: our **isolate-per-tab-in-shared-process** model (C1) means the OS won't hand us per-tab CPU/RAM for free (unlike Chrome's process-per-tab). SpiderMonkey **does** support per-compartment/zone memory attribution (Firefox's `about:memory` reports per-JS-compartment; every GC cell belongs to ≤1 compartment) — but with a caveat that ~30–45% lands in "heap-unclassified" (imperfect attribution) (firefox-source-docs js/gc; blog.mozilla.org nnethercote). *Disposition:* **confident — do it, reuse SpiderMonkey per-compartment memory reporters** for per-tab JS heap + **hand-roll** compositor per-tab layout/paint/tile accounting + a shell task-manager UI. It rides on the same instrumentation **C1 + §8** need; the shared-process **self-attribution** (esp. per-tab *CPU*, which needs self-timing with no per-process split, and the heap-unclassified gap) is the wrinkle to solve there, not a blocker. *Roadmap:* §10 (+ C1, §8, §1). No WPT target.

## Suggested crate layout

```
/engine
  /net        — hyper, quinn, tokio-tungstenite, rustls integration
  /html       — html5ever integration
  /css        — Stylo integration
  /layout     — taffy + custom block/inline/table/positioned/float layout, WPT-verified
  /js         — mozjs bindings, DOM/Web API surface, WPT-verified per API
  /text       — Parley, swash, fontdb
  /paint      — Vello integration (GPU/CPU/Hybrid tiers)
  /compositor — layer compositing, damage tracking, hibernation logic
/shell        — winit-based GUI: window, tabs, navigation, chrome
/tests
  /wpt        — WPT test harness and results tracking
```

## Working agreements

- Any change touching `/engine/layout`, `/engine/css`, or `/engine/js` should be checked against the relevant WPT subset before being considered complete — cite which tests were run.
- Any change touching binary size or per-tab memory should report before/after numbers.
- Treat SpiderMonkey as a vendored dependency you configure and bind against, not a codebase you patch internals of — see Modification boundaries above.
- When in doubt about whether a site belongs in the "broadest possible, excluding hostile" target set or the excluded tier, default to excluding rather than building bot-evasion logic into the core engine.
- **Documentation is part of "done."** On every major change — a new crate, a new capability, a wired feature seam, or a changed public API — update **both** `README.md` and this file in the same change. `README.md` must describe the stack *as it actually is* (no aspirational claims in the per-crate status; move aspirations to "Directions of improvement" below). Re-read and re-prioritize the "Current state" and "Directions of improvement" sections here periodically.
- The agent's inference is provider-agnostic: keep it behind the `InferenceBackend` trait (Groq is one implementation). Never commit API keys, `.env`, or the local-only development harness.